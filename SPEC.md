# Image Reverb — 產品規格書 (Spec)

版本：v0.5.3｜日期：2026-09-24｜狀態：Phase 1 兩次 MVP 驗收 FAIL（T-17、T-17-R2，永久保留）→ Phase 1.10 轉向「參數為主、照片為輔」；**v0.5.1：§7 加入判準 v2（輔助路徑，`criteria: T-63 v2`）；v0.5.2：§7-v2 依判準 v2.1 改寫 8 行（`criteria: T-63 v2.1`）；v0.5.3：§7-v2 依判準 v2.2 改寫 2 行（`criteria: T-63 v2.2`）**

> v0.5 變更（使用者 2026-09-21 決定；Fable 裁決 T-17-R2-A，全文見 TASKS.md「Phase 1.10」）：
> **產品方向改為「參數為主、照片為輔」**——產品核心是空間類型／尺寸／逐面材質這組參數（可選 preset、可手調）；
> 照片分析的角色由「全自動出 IR」改為「先幫使用者填好建議值」，**使用者確認或修改後才出 IR**。
> 依據：全自動照片路徑兩次驗收未達（R2：18 個 run 有 16 個被 gate 擋下；放行的 2 張材質可判面錯 8/10、估計尺寸系統性偏大），
> 而「給定尺寸＋材質 → IR」的引擎可用。使用者 2026-09-16 原話：「軟體內部有相關空間或材質的參數即可，不需要真的將圖片變成選項。」
> 本版改動：§1 定位、§2 流程、F-09 說明、新增 F-18、F-23 註記、§8 新增一列風險。
> **§7 驗收標準本版一字未動**：新判準（輔助路徑）草案在 TASKS.md T-63，須經獨立 Opus 審條文＋使用者核准＋獨立 `criteria:` commit 才會寫進 §7；在那之前 §7 原文有效。

> v0.4 變更（使用者需求新增，Fable 規劃 2026-08-27）：
> 新增 §3.4「Phase 1.5 — 場景描述輸入與複合場景」（F-16 文字場景輸入、F-17 複合場景）。
> 架構上兩者都掛在既有中間表示（`AcousticsResult`）之上：文字輸入是新的前端、
> 複合場景是 IR 引擎之上的路徑串接層，照片管線（F-01~F-05）與 IR 引擎（T-14）不改。
> 任務卡 T-20/T-21，詳見 TASKS.md。

> v0.3.1 變更（T-11 決策補丁複測後的 Fable 裁決）：
> 環景路徑的量程規則實測會觸發於 Steinman Hall（三面原始牆距 12.2/10.4/11.1m 超過 10m）
> ——裁決：這是規則該有的行為，規則不改。明確化：**環景解「視野外」、不解「量程」**；
> 單面牆距超標的環景場地同樣走 `confidence: low` ＋手動尺寸出口。
> §7-2 驗收措辭同步由「範圍外透視場地」改為「範圍外場地（透視或環景單面牆距超標者）」。
> 裁決全文：TASKS.md T-11 卡「Fable 裁決」、`output/geometry/REPORT.md` §8.5。
>
> v0.3 變更（T-11 評測關卡卡關後的 Fable 路線決策）：
> 自動幾何估計明訂適用範圍「一般室內、估計最大尺寸 ≤ 10m」（F-02）——
> metric depth 模型實證量程天花板 ~20m 且天花板前就開始壓縮（走廊 30m 被壓成 12.8m），
> 估值 >10m 即不可信；超出範圍是正式行為分支：`confidence: low` ＋顯性警示，
> 出口為手動尺寸（F-09）或環景輸入。±30% 判準數值不變，改的是適用域。
> 驗收 §7-2 同步標明範圍外透視場地以手動尺寸跑後半段管線。
> 實證出處：`output/geometry/REPORT.md`、TASKS.md T-11 卡「Fable 路線決策」。
>
> v0.2 變更（T-08 路線決策，依 Phase 0 實測結果修訂）：
> 深度改走 metric depth（F-02）、材質改兩階段辨識且逐表面指定（F-03、§6）、
> RT60 明文禁止平均 α 算寬頻值（F-04）、手動覆寫升 P0（F-09）、
> 前處理層納入環景投影（§5）、驗收加入人耳試聽（§7）、風險表更新（§8）。
> 實證出處：`output/depth/REPORT.md`、`output/seg/REPORT.md`、TASKS.md T-03 交接筆記。

## 1. 產品定位

**一句話**：給一張照片（或影片），AI 自動分析空間幾何與材質，生成該空間的
Impulse Response（IR），提供擬真的 Convolution Reverb 空間模擬。

**對標產品**：Audio Ease Altiverb 8（sampled acoustics processor）。
差異：Altiverb 的 IR 依賴實地錄製；Image Reverb 的 IR 由 AI 從影像生成，
任何拍得到的空間都能「進去混音」。

**v0.5 定位補充（參數為主、照片為輔）**：產品交到使用者手上的是一組**看得懂、改得動的空間參數**
（空間類型、長寬高、六面材質）與由它生成的 IR。照片是最快的起點——系統從照片提出建議值並標出
「哪幾格最可能錯、請先看」——但**最後拍板的是使用者**。測試用的照片與對照 IR 只是開發素材，
不隨產品發佈，也不會變成產品裡的選項；產品內建的是空間／材質的**參數**（preset），不是圖片。

**目標使用者**：

| 使用者 | 情境 |
|--------|------|
| 影視聲音後期 (post-production) | 對白 ADR 需要貼合畫面中場景的殘響（最強需求：畫面就是輸入） |
| 音樂混音師 | 想要特定真實空間的音色，但買不到/找不到該空間的 IR |
| 遊戲/互動音訊設計師 | 用場景截圖快速產生環境殘響 |
| 一般創作者 | 拍張照就能讓聲音「在那個空間裡」 |

## 2. 核心使用流程 (User Flow)

```
0.（v0.5）也可以不給照片：直接選空間 preset 或輸入文字描述（F-16），跳到步驟 2b
1. 使用者拖入一張照片（或影片片段）
2. 系統顯示分析結果——**v0.5 起這是「建議值」，不是定案**：
   - 空間類型（音樂廳 / 教堂 / 客廳 / 浴室 / 戶外…）
   - 估計尺寸與體積
   - 辨識出的表面材質（疊加在影像上的視覺化）
   - 推估聲學參數：RT60（分頻段）、pre-delay、早期反射密度
2b.（v0.5）使用者確認或修改尺寸與逐面材質；系統把低信心的欄位排在最前面提醒（F-18）。
   **未經確認的欄位不會被拿去出 IR**
3. 系統由確認後的參數生成 IR，使用者即時試聽（內建 dry 素材或自己的音訊）
4. 使用者微調參數（Wet/Dry、Size、EQ/Damping、Pre-delay…）
5. 匯出 IR (WAV) 或直接在 DAW plugin 中使用
```

## 3. 功能需求

### 3.1 MVP（Phase 1）— 「照片 → IR」桌面工具

| ID | 功能 | 說明 | 優先級 |
|----|------|------|--------|
| F-01 | 照片輸入 | JPG/PNG/HEIC，單張 | P0 |
| F-02 | 空間幾何估計 | 單目 **metric depth** 估計（Depth-Anything-V2-Metric-Indoor 類）→ 房間尺寸、體積。**適用範圍（T-11 實測定案，2026-08-25）：一般室內、估計最大尺寸 ≤ 10m**（模型量程天花板 ~20m、天花板前即開始壓縮，估值 >10m 不可信；門檻在 `config.GEOMETRY_SCOPE_MAX_M`）。±30% 誤差目標僅在範圍內適用；**範圍外（含車內/超大空間）是正式行為分支**：必須輸出 `confidence: low` ＋可操作警示（建議手動尺寸 F-09 或環景輸入），不得給出自信的錯誤數字。環景路徑量程規則獨立（單面牆距 >10m 才降 low）。相對深度路線已被 T-05 實測否定，不得回頭使用 | P0 |
| F-03 | 材質辨識 | **兩階段**：語意分割只負責切出表面的幾何角色（牆/地板/天花板/大面積物件），材質標籤由**區域級二階分類器**判定（ADE20K 的 `floor`/`wall` 類別語意經 T-06 實證不可信）。**吸音係數逐表面指定，禁止全域套用單一材質**（T-03 實證：全鋪地毯 vs 只有地板地毯，低頻 RT60 差 11.8 倍） | P0 |
| F-04 | 聲學參數推估 | RT60 六個八度頻段（125/250/500/1k/2k/4k Hz）**逐頻段獨立計算，禁止用平均 α 算單一寬頻值**（T-03 實證：平均法差 15 倍）、pre-delay、直達/殘響比 | P0 |
| F-05 | IR 生成 | 由聲學參數合成 IR；MVP 路線見 §5 | P0 |
| F-06 | IR 匯出 | WAV（48kHz/24bit，mono 與 stereo），可直接載入 Altiverb 等 convolution reverb | P0 |
| F-07 | 離線試聽 | dry 音檔 + IR → wet 音檔（離線卷積） | P0 |
| F-08 | 分析結果視覺化 | 材質分割疊圖、參數列表 | P1 |
| F-09 | 參數手動修正 | 使用者可覆寫尺寸與逐表面材質（**P0**——Phase 0 證明分析模型會「安靜地輸出看似合理的錯誤結果」，手動覆寫是可靠性底線）；RT60 直接覆寫為 P1。**v0.5：由「補救出口」升為主流程的一環（見 F-18）** | P0 |
| F-18 | 建議→確認 兩段式（v0.5 新增） | `--suggest`：照片 → **參數檔**（建議尺寸、六面材質、各欄信心與「請先看」旗標；不產生任何 IR）；`--params`：由使用者確認／修改後的參數檔出 IR（不載入任何分析模型）。尺寸或任一面未標記確認 → 拒絕輸出並列出待確認欄位，`--force-low-confidence` 不得繞過。參數檔同時是 Phase 2 plugin UI 的資料模型。照片直通路徑（F-01～F-05＋gate）保留、行為不變。任務卡 T-65 | P0 |

### 3.2 Plugin（Phase 2）

| ID | 功能 | 說明 | 優先級 |
|----|------|------|--------|
| F-10 | 即時卷積引擎 | Partitioned convolution，零延遲模式 | P0 |
| F-11 | AU / VST3 | macOS 優先，Windows 次之 | P0 |
| F-12 | 基本參數 | Wet/Dry、Gain、Pre-delay、Size（IR 時間伸縮）、高/低頻 Damping、EQ | P0 |
| F-13 | 照片載入 UI | plugin 內直接拖入照片 → 呼叫分析管線 | P0 |
| F-14 | IR 庫瀏覽器 | 內建預生成 IR + 使用者生成的 IR 管理 | P1 |
| F-15 | Stereo/多聲道 IR | 由 mono IR 衍生去相關 (decorrelated) stereo；true stereo 為後續目標 | P1 |

### 3.3 場景描述輸入與複合場景（Phase 1.5，2026-08-27 使用者需求新增）

| ID | 功能 | 說明 | 優先級 |
|----|------|------|--------|
| F-16 | 文字場景輸入 | 不用照片，直接用文字描述場景產生 IR（如「大教堂」「4x3x2.5 的房間，地板鋪地毯」）。**v1 實作＝場景 preset 庫（`data/scene_presets.json`）＋中文/英文關鍵字比對＋顯式參數抽取（尺寸 `長x寬x高`、材質關鍵字、大小修飾詞）**，輸出與照片管線相同的中間表示（`dims_source: "text_description"`）後接 T-13/T-14。**不接外部 LLM API**（隱私原則 §4：本機執行；本機 LLM 留待 Phase 3 評估）。認不得的描述必須報錯並列出可用場景與格式範例，**不得安靜猜一個**（Phase 0 三次實證的失敗型態）。非建築空間（車內等）明確拒絕並導向照片＋手動覆寫 | P0 |
| F-17 | 複合場景 | 聲源與聽者在**不同空間**、聲音經一或多條傳輸路徑到達（例：巨蛋演唱會→通道走廊聽；隔壁講話聲經牆/窗/走廊混合傳來）。**v1 實作＝路徑串接近似（path cascade）**：`IR = Σ路徑 [聲源空間IR ⊗ 傳輸濾波(牆體TL/開口) ⊗ 中繼空間IR(可選)] ⊗ 聽者空間IR`，各路徑有獨立增益/延遲/逐頻段傳輸損失（`data/transmission.json`，建築聲學公開 TL 值＋出處）。場景以 JSON 描述（空間可引用 F-16 的 preset）。**這是工程近似而非完整耦合房間模擬**——輸出 JSON 必須標 `method: "path_cascade_v1"` 與近似聲明；每次改動必過人耳試聽（§7 第 4 條） | P0 |

**Phase 1.5 的定位**：兩者都不動照片管線與 IR 引擎——F-16 是 `AcousticsResult` 的新產生器，
F-17 是 IR 引擎輸出之上的組合層。依賴 T-14（IR 合成引擎）；在 T-15（CLI 整合）之前完成，
讓 T-15 一次把三種輸入（照片/文字/複合場景 JSON）都收進 CLI。

### 3.4 進階（Phase 3+）

- F-20 影片輸入：多幀融合提升幾何/材質準確度；鏡頭移動時的動態空間
- F-21 Positioner：在空間中移動音源位置（改變早期反射與直達聲）
- F-22 神經 IR 生成：以影像為條件的生成模型路線（取代或混合參數化合成）
- F-23 空間類型 preset 庫：常見空間預生成 IR（v0.5 註：preset＝**參數**〔`data/scene_presets.json`＋`data/materials.json`〕與由參數生成的 IR，不含任何照片；測試照片不隨產品發佈）

## 4. 非功能需求

| 項目 | 目標 |
|------|------|
| 分析時間（照片 → IR） | MVP ≤ 60 秒（本機 GPU/Apple Silicon）；目標 ≤ 10 秒 |
| Plugin 即時性能 | 零延遲模式；CPU 佔用與市售 convolution reverb 同級 |
| IR 品質指標 | 與真實 IR 對比：RT60 每頻段誤差 < 20%；EDT、C50/C80、盲聽 A/B 測試 |
| 隱私 | 影像分析預設在本機執行，不上傳雲端 |
| 平台 | macOS (Apple Silicon) 優先；Windows Phase 2 之後 |

## 5. 系統架構

```
┌─ 影像分析管線 (Python / PyTorch) ────────────────────┐
│ 照片/影片                                             │
│   ├─ 前處理：letterbox/UI 裁切、環景偵測 →            │
│   │   equirect→多視角透視投影（環景照展開成透視圖）    │
│   ├─ 深度估計 (metric depth：Depth-Anything-V2-       │
│   │   Metric-Indoor 類，輸出公尺而非相對值)           │
│   ├─ 表面分割（語意分割只取幾何角色：牆/地板/天花板）  │
│   │   └─ 材質二階分類器（對每個表面區域判材質，       │
│   │       含信心值 gating 與 fallback 警示）          │
│   └─ 空間類型分類                                     │
│         ↓                                            │
│ 聲學參數推估器                                        │
│   （體積、逐表面 Sᵢ×αᵢ → Sabine/Eyring →             │
│     逐頻段 RT60，禁止平均 α）                         │
└──────────────┬───────────────────────────────────────┘
               ↓  acoustic params (JSON)
┌─ IR 生成引擎 ─────────────────────────────────────────┐
│ 路線 A：幾何聲學模擬 (pyroomacoustics image-source)    │
│ 路線 B：參數化合成（頻段 shaped-noise / FDN）＋        │
│         早期反射由簡化幾何生成 ← MVP 採 A+B 混合       │
│ 路線 C：神經生成（Phase 3 評估）                       │
└──────────────┬───────────────────────────────────────┘
               ↓  IR (WAV)
┌─ 播放/卷積層 ────────────────────────────────────────┐
│ MVP：離線卷積 (Python)                                │
│ Phase 2：JUCE (C++) partitioned convolution plugin    │
│          （Python 管線經 local service 或模型移植呼叫）│
└──────────────────────────────────────────────────────┘
```

**MVP IR 生成決策（暫定）**：早期反射用簡化房間幾何跑 image-source（路線 A），
晚期殘響用分頻段指數衰減 shaped-noise 合成（路線 B），交叉淡接。
理由：可控、可解釋、不依賴訓練資料；神經路線（C）先做研究評估（見 RESEARCH.md）。

## 6. 材質 → 吸音係數

建立材質對照表（125Hz–8kHz 各頻段吸音係數 α），來源為建築聲學公開資料。
初版涵蓋：混凝土、磚、木板、石膏板、玻璃、大理石/磁磚、地毯、布幕/織物、
吸音板、人群/座椅、草地/泥土（戶外）。

材質判定規則（T-06 實測後修訂）：

- 分割模型的類別**只用來切出表面範圍與幾何角色**；`floor`/`wall` 的材質語意不採信
  （滿鋪地毯 70.4% 被判成 `floor`，換算後高頻吸音只剩正確值的 32%）。
- 每個表面區域交給**二階材質分類器**判定材質 id（v1 用 CLIP zero-shot 對映 materials.json 的類別）。
- 分割中語意可信的類別（mirror、window、curtain 等 🟢 級，見 `output/seg/REPORT.md` §4）可直接映射。
- 無法辨識或信心不足 → fallback `generic_wall`，**且必須在分析報告中標示警示**，不得安靜 fallback。
- **吸音係數逐表面指定**（地板/天花板/各面牆分開），資料結構與 IR 生成引擎都不得假設單一全域材質。

## 7. 驗收標準（MVP）

> v0.5 註：本節條文**未改**。「輔助路徑」判準 v2 草案見 TASKS.md T-63（核准前不生效）。依本節的兩次驗收結果：T-17 `MVP：FAIL`、T-17-R2 `MVP：FAIL（R2）`，永久保留。
> v0.5.1 註（2026-09-23；`criteria: T-63 v2`）：**自 T-17-R3 起，MVP 軸依本節「§7-v2」判定**；下列 1～4（v1，全自動照片路徑）原字保留，T-17／T-17-R2 依 v1 作成的 FAIL 永久有效、不得覆寫。§7-v2 條文＝TASKS.md T-63 卡「判準 v2 草案 修訂版（r5）」R5-2 逐字寫入（Opus 審 r5 結論「可送使用者核准」`ad340cb`；使用者 2026-09-23 核准原話「T-63 r5 核准」），只機械刪除修訂標籤 `〔r2：…〕`～`〔r5：…〕`、`〔回應…〕` 與共同行首縮排；R5-2 的「讀法」為結構行、不寫入。Opus r5 意見 J-1～J-6（無〔擋〕）依 V2-2(d)⑤ 程序以 `criteria: T-63 v2.1` 另補，須早於 T-70 步驟 3b 與任何 R3 量測。
> v0.5.2 註（2026-09-24；`criteria: T-63 v2.1`）：§7-v2 依 TASKS.md T-63 卡「判準 v2.1 草案」R6-2 **改寫 8 行**（Opus r5 意見 J-1～J-6；改寫前的行號 210、216、217、230、231、233、242、247，本註插入後各往下移 1 行），無新增行、無刪除行、無搬移，§7-v2 仍 72 行；只機械刪除修訂標籤 `〔v2.1：…〕`；門檻數字一個都沒動、整段沒有新增任何數字；v1 條文與 T-17／T-17-R2 的 FAIL 不受影響。程序＝V2-2(d)⑤（Opus 審 v2.1 結論「可送使用者核准」`814e655`；使用者 2026-09-24 核准原話「T-63 v2.1 核准」）。改寫後 §7-v2 72 行的 sha256＝`d387377252be82cf3cf271402bb766e8cf105134a560a18fbe6d4940b8828368`（`'\n'.join`、不含結尾換行；比對時比內容、不比行號）。Opus v2.1 意見 K-1（warnings 檢查不符合時的處置）依同一程序以 `criteria: T-63 v2.2` 另補，須早於 T-17-R3 開卡與任何 R3 量測；在此之前 T-70 3b 已可開跑（v2.1 是 3b 的前置）。
> v0.5.3 註（2026-09-24；`criteria: T-63 v2.2`）：§7-v2 依 TASKS.md T-63 卡「判準 v2.2 草案 修訂版（r3）」R9-2（第 231 行）＋「修訂版（r2）」R8-2（第 243 行）**改寫 2 行**（Opus v2.1 意見 K-1「warnings 檢查不符合時的處置」——(甲) 事前自檢（含 `import numpy` 之後的 `np.geterr()` 第二段）／(乙) 事後發現依「該次執行算了什麼」分路；Opus v2.1 第 13 點——同一房間麥克風數不唯一 → 停；改寫前的行號 231、243，本註插入後各往下移 1 行），無新增行、無刪除行、無搬移，§7-v2 仍 72 行；只機械刪除修訂標籤 `〔v2.2：…〕`；門檻數字一個都沒動、整段沒有新增任何數字；v1 條文與 T-17／T-17-R2 的 FAIL 不受影響。程序＝V2-2(d)⑤（Opus 審 v2.2 r3 結論「可送使用者核准」`ce35357`；使用者 2026-09-24 核准原話「T-63 v2.2 核准」；r1／r2 分別由 `f5e586a`／`9e90146` 退回後修訂）。改寫後 §7-v2 72 行的 sha256＝`31ad16c9466b39b99a78763c59082b07b0b60bf908ff5d4ed90a8da1c0d8fbd9`（`'\n'.join`、不含結尾換行；比對時比內容、不比行號）。v2.2 是 T-17-R3 開卡與任何 R3 量測的前置；不是 T-70 3b 的前置。Opus v2.2 r3 的卡片層級意見 N-1／N-2／N-3 只寫入 T-17-R3 卡（收緊、不動本節）。

1. 拖入 5 種代表性空間照片（浴室、客廳、教堂/大空間、走廊/樓梯間、車內），
   生成的 IR 在盲聽下能被聽者正確配對到空間類型（≥ 4/5）
2. 對有真實 IR 對照的空間（EchoThief + MIT Reverb Survey，共 8 場地；OpenAIR 已停站），
   生成 IR 的 RT60 各頻段誤差 < 20%。環景場地照片經前處理投影後全部可用；
   **超出 F-02 適用範圍的場地——透視照任一維超標，或環景單面牆距超標
   （如 MIT 三張大空間照片、Steinman Hall）——以手動尺寸（F-09，
   標 `dims_source: manual`）跑後半段管線——這是產品對超範圍輸入的正式行為，
   驗收報告必須逐場地標明尺寸來源**；
   必測反例 `racquetball_court_4`（小空間、長殘響 3.538s）不得被「小空間=短殘響」類規則做錯
3. 匯出的 WAV 可直接載入 Altiverb / Space Designer / 任一 convolution reverb 正常使用
4. **人耳試聽驗收**：每次改動 IR 生成邏輯後，必須產生試聽檔（convolve.py）請使用者實際聽過。
   Phase 0 實證：錯 15 倍的模型缺陷通過了全部三層數值檢查，只有人耳抓得到。數字合理 ≠ 聽起來對

### §7-v2 MVP 判準 v2：輔助路徑（`criteria: T-63 v2`；2026-09-23 生效；受驗路徑＝`照片 → --suggest 參數檔 → 使用者確認／修改 → --params 出 IR`；門檻數字與 v1 相同：≥4/5、<20%、500Hz／1k／2k／4k 逐頻段＋88.4–353.6 Hz 聯合帶、名單全部場地五項皆達、名單 <3 記未達）

- **程序 P（參數鎖定）**
  - P-1 **先鎖後出**：每一個受驗或報告項 run 的 `params.json`，在**任何**會合成 IR 的 `--params` 執行之前，一次全部獨立 commit（訊息 `docs: T-17-R3 參數鎖定`；存放路徑由 T-17-R3 卡定，須進版控、單一目錄、檔名＝場地代號）。**鎖定 commit 必須包含下列全部場地的參數檔**：(1) `assets/photos_synth_heldout/ASSET_MANIFEST.json` 所列 held-out **全部**（不只盲測五個代表；V2-5 報告項要用）；(2) V2-2(e) 第一段鎖定的候選名單全部（`mit_gym`＋T-70 `rooms.json` 的 `included` 區所列房間）；(3) V2-2(g)(h) 的報告項場地 4 個（`mit_department_store`、`mit_restaurant`、`racquetball_court_4`、`steinman_hall`；`mit_gym` 已含於 (2)）。 **鎖定 commit 的唯一認定**：鎖定 commit＝`main` 上**第一個**訊息以 `docs: T-17-R3 參數鎖定` 開頭的 commit（機械取法：`git log --reverse --format='%H %s' main | grep -m1 -E '^[0-9a-f]{40} docs: T-17-R3 參數鎖定'`）；其後的任何 commit 不論訊息為何，一律以 P-3 偵測指令判定，不得宣稱為鎖定 commit；R3 REPORT 與獨立複驗者各貼一次該指令輸出。
    **漏鎖＝未達**：上列任何一個場地的參數檔不在鎖定 commit 內 → 該場地依 P-3 處理（名單場地五項全記未達；盲測代表該題記答錯；其餘 held-out 與報告項場地標「作廢（漏鎖）」並在 REPORT 列出）；**不得**因此把該場地移出名單或縮小分母。機械驗證＝`git show --name-only --format= <鎖定 commit>` 的檔名清單 ⊇ 上述三份清單的全部代號（R3 REPORT 貼輸出，獨立複驗者重做）。 **參數檔照片指紋核對**：另逐檔核對鎖定 commit 內每一個參數檔的 `photo.sha256`＝該場地 `--suggest` 實際輸入照片的現算 sha256（只算 sha256，鐵則 18 允許；照片路徑由 T-17-R3 卡在鎖定 commit 之前逐場地明列：held-out＝`assets/photos_synth_heldout/ASSET_MANIFEST.json` 所列該場地 `photo.png`、名單公開房間＝該房間 `rooms.json` 的 `suggest_photo` 欄位所記那一張（＝(a)(iii) 末句選定者；欄位＝相對路徑＋sha256，T-70 3b 寫入、經 T-70 的 Opus 驗證後寫死；核對時以該欄位的相對路徑現算 sha256，現算值須同時等於該欄位所記 sha256 與參數檔 `photo.sha256`）、`mit_gym`＝`assets/reference_irs/mit_gym/site_photo_gym.png`、(g)(h)＝T-17-R3 卡明列的那一張），在鎖定 commit 之後、任何 `--params` 之前執行一次，逐檔指令與輸出貼 R3 REPORT、獨立複驗者重做；任一檔不一致（含拿別場地的參數檔當範本改值而帶錯指紋）→ 該場地依 P-3（名單場地五項全記未達；盲測代表該題記答錯；報告項場地標「作廢」），不得改檔重鎖、不得因此把該場地移出名單或縮小分母。
  - P-2 **只認鎖定版**：盲測樣本與所有量測 IR 只能由鎖定版參數檔產生——盲測 MANIFEST 與 RT60 表逐筆記 `params_sha256`＝該 commit 內檔案的 sha256，且等於 `analysis.json` 的 `params.file_sha256`；IR 的 provenance 時間必須晚於鎖定 commit 時間。驗證＝Opus 以 `git show <鎖定 commit>:<路徑> | shasum -a 256` 逐筆比對。
  - P-3 **鎖定後改檔＝作廢**：鎖定後任何參數檔被修改 → held-out 該題以答錯計；名單場地該場地五項全記未達；報告項場地標「作廢」。
    **P-3 偵測指令**：`git log --oneline <鎖定 commit>..HEAD -- <參數目錄>` 必須為空；非空＝有參數檔在鎖定後被修改或補放，逐檔依 P-3／P-1「漏鎖」處理。R3 REPORT 貼此指令輸出，獨立複驗者重做。
  - P-4 **不得重跑補樣**：以鎖定檔執行 `--params`，exit ≠ 0 或未產生 `ir_mono.wav` → 同 P-3；不得改檔重跑、不得 `--force-low-confidence` 補樣。**唯一例外＝同時符合下方「定義 RR」五條的環境故障重跑**；不符任何一條的失敗一律照 P-3，不得由執行者自行認定為「環境故障」。
  - **定義 RR（環境故障重跑；P-4 與鐵則 18-a(3) 共用這一條，兩處不得各自解釋）**：某次執行失敗後，**同時**符合下列五條才可重跑——
    RR-1 **未留下輸出檔**：失敗那次執行的目標位置（`--params` 的 `--out-dir`；`--suggest` 的 `--out-params`；鐵則 18-a(2)②③ 的輸出目錄；引擎側量測工具（V2-2 比較式對生成 IR 呼叫估計式的 R3 工具）的輸出目錄——由 T-17-R3 卡事前指定為 `output/mvp_acceptance_r3/` 下與上列子目錄及 `run_evidence/` 互不重疊的**一個**子目錄，RR-1 對它只比對那一個子目錄）在失敗後的檔案清單與執行前完全相同（沒有任何新檔，含半寫入檔）。留下任何檔 → 不得重跑，照 P-3。 **目標位置不含 `output/mvp_acceptance_r3/run_evidence/`**——RR-5 把紀錄寫進 `run_evidence/` 不算「留下輸出檔」；18-a(2)①②③ 的輸出目錄由 18-a(r3)(2) 指定為 `output/mvp_acceptance_r3/` 下與 `run_evidence/` 互不重疊的子目錄（`params_suggested/`、`auto_runs/`、`reference_metrics/`），RR-1 只比對那一個子目錄。 **18-a(2)④（獨立複驗）的目標位置**＝複驗者在第一次執行前於其複驗紀錄中宣告的**一個** scratchpad 子目錄（絕對路徑逐字記入紀錄；宣告後不得更換），RR-1 對 ④ 只比對該目錄。
    RR-2 **故障類別可機械判定，只接受兩類**：(A) 行程被訊號中止＝shell 取得的 exit code ≥128；(B) stderr **最後一個非空行**（機械取法：`grep -v '^[[:space:]]*$' <stderr 檔> | tail -n 1`）符合下列之一：(B-1) 以 `OSError: [Errno 28]`（ENOSPC，磁碟滿）、`OSError: [Errno 5]`（EIO）或 `OSError: [Errno 12]`（ENOMEM）開頭；或 (B-2) 該行第一個 `:` 之前的文字（整行無 `:` 則取整行），取其最後一個 `.` 之後的部分（無 `.` 則取全部）恰為 `MemoryError`（機械取法：該行經 `awk -F: '{print $1}' | awk -F. '{print $NF}'` 輸出恰為 `MemoryError`）。**程式內部不得攔截本款類別的例外**：R3 工具（含 (e)、(k) 的逐支迴圈與引擎側量測）遇到屬 (B) 類別的例外（程式判斷式見 (e)）須原樣重新拋出使行程失敗，才有 stderr 可依本款判定；被攔截、改寫或轉印成其他訊息者不得事後宣稱為環境故障。**所有依本款須原樣重新拋出的 R3 工具**（含 (e)、(k) 的工具與引擎側量測工具）都須在全部項目算完後才一次寫出結果檔（失敗時不留半寫入檔，RR-1）；(e) 內「全部支、全部頻段算完後才一次寫出」是本規定對 (e) 工具的重述、不是限定。其他任何失敗（exit 2、exit 3、其他 traceback）都**不是**環境故障。
    RR-3 **程式碼相同**：失敗那次與重跑那次，執行前各記一次 `git rev-parse HEAD`（兩次必須相同）與 `git status --porcelain -- src data scripts`（兩次皆為空；限定這三個路徑，是因為 R3 自己的新輸出檔會讓全域 porcelain 非空）**與 `pip freeze | shasum -a 256`（在 `.venv` 啟用下執行；兩次雜湊必須相同）**——三者任一不同 → 不得重跑；重跑成功後輸出檔 `provenance` 的 commit 欄必須等於該 HEAD。 **環境指紋**：R3 第一次執行任何 18-a(2) 程式之前，把 `pip freeze` 全文與其 sha256 存入 `output/mvp_acceptance_r3/run_evidence/env_freeze.txt`／`env_freeze.sha256`，並把 `python -VV` 的輸出（在 `.venv` 啟用下執行）逐字存入同目錄 `env_python.txt`；之後每次執行（含重跑、含獨立複驗）都先與之比對（雜湊相同且 `python -VV` 輸出逐字相同）並把當次雜湊與 `python -VV` 輸出記進該次執行的紀錄；任一不同 → 視同 RR-3 不成立。
    RR-4 **輸入相同**：重跑使用同一 sha256 的參數檔／封存檔。
    RR-5 **次數與紀錄**：每個（輸入檔 × 程式）最多重跑 **2 次**（連同首次共 3 次執行）；第 3 次仍失敗 → P-3。18-a(2)④（獨立複驗）對 ③ 的重算即使是同一支程式，也與 ③ **分開計次**（④ 自己最多重跑 2 次；④ 的次數不佔用 ③ 的、③ 的也不佔用 ④ 的）。每次執行（含失敗）的時間、HEAD、完整指令、exit code、stderr 全文存 `output/mvp_acceptance_r3/run_evidence/`（④ 的存於其宣告的 scratchpad 子目錄旁的複驗紀錄），REPORT 逐次列出。
  - P-5 **順序與資訊隔離**：① 參數鎖定 commit → ② V2-2(e) 參考值可算性檢查 commit → ③ 才可執行任何 `--params`。①完成前，參數確認者（使用者）與協助確認的任何視窗不得讀取、計算、轉述名單場地與報告項場地的真實 IR、其 RT60／T30、或資料集論文中的殘響時間表（`mit_gym`／`racquetball_court_4` 等 R2 已公開者見 V2-2(b)(g) 的已知限制句）。
    **P-5 已知限制（REPORT 逐字）**：「鎖定前是否有人以未鎖定的參數私下試跑、試聽後再鎖定，無法機械排除；本輪依執行視窗的聲明與下列指令輸出。」R3 執行視窗在鎖定 commit **之前**貼出 `find <本視窗 scratchpad> output -name ir_mono.wav -newermt "<T-63 criteria commit 的 committer 時間>"` 的輸出：必須為空；非空時**逐筆機械判定**——**照片 sha256 取法**：讀命中檔同目錄 `analysis.json`，`params` 為 JSON 物件（非 `null`；＝`--params` 路徑產物）時取 `params.photo_sha256`（T-65 r3-1：逐字等於參數檔 `photo.sha256`）；`params` 缺鍵或值為 `null`（＝照片路徑產物）時取 `provenance.input_sha256`；`params` 為物件但沒有 `photo_sha256`、或依上述取不到任何 sha256 → 比照「半寫入」處理（見本段末句）。以該 sha256 對照三份清單的照片 sha256：held-out＝`assets/photos_synth_heldout/ASSET_MANIFEST.json` 所列全部；名單場地＝`assets/reference_rooms_heldout/ASSET_MANIFEST.json` 所列全部影像檔＋`assets/reference_irs/mit_gym/` 內的照片（現算 sha256）；(g)(h)＝`assets/reference_irs/{mit_department_store,mit_restaurant,racquetball_court_4,steinman_hall}/` 內的照片（現算 sha256）。對不上 → 放行並在 REPORT 列出；對得上 → 該場地依 P-3，**唯一例外＝T-65 的開發用產物**，須**同時**滿足 (i) 照片 sha256 ＝ `assets/reference_irs/mit_gym/site_photo_gym.png` 的 sha256（`9ccc9c335ff8862a7f46f22548c1d1c90e9b4b79090cfaf9dd25f2346a96749e`；R3 執行時現算核對）；(ii) `analysis.json` 的 `provenance.cli_params.override_dims` 解析後等於 9.00×6.00×2.90 且 `override_materials` 六面逐欄等於 T-65 卡步驟 0 的固定等價性測試值（`floor=rubber_flooring`、`ceiling=metal_roof_deck`、`west=east=concrete`、`south=north=gypsum_board`），或 `--params` 路徑的 `analysis.json` 所記參數檔（依 `params.file_sha256` 找到的那一份；找不到該檔 → 例外不成立）尺寸與六面逐欄等於同一組值（T-65 步驟 3）——這種以外、出自上述照片的任何命中檔一律 P-3。命中檔旁沒有 `analysis.json`，或 `analysis.json` 依上述取法取不到 sha256（半寫入）→ 以其父目錄名對照各場地代號與照片檔名主幹，對得上者 P-3、對不上者列出。
    **P-5 `--check`**：鎖定前允許（並建議）對每個參數檔執行 `--params <檔> --check`（T-65 r2-3：只驗 schema／確認旗標／退化規則；不合成 IR、不寫任何檔、不印任何聲學量）；`--check` 不算 P-1／P-5 所指的 `--params` 執行。如實註明：T-65 退化規則 2（六面同材質 → exit 3）在 P-4 下會使「照實確認為六面同材質」的場地作廢；`--check` 只讓使用者在鎖定前知道這件事，不改變該規則。
  - P-6 **允許照抄**：使用者可以直接採用素材卡的設計尺寸（合成圖）或官方文件尺寸（公開資料集）當確認值——這是設計內（R3 量的是「引擎在已知參數下的表現」），不算洩題。盲聽樣本只給音檔；作答先 commit 再開答案鍵（R2 程序）。
- **V2-1 盲聽**：≥4/5 不變。held-out＝T-64 路徑 S 封存於 `assets/photos_synth_heldout/` 的 AI 合成圖；五題＝五類各取該類 held-out 中 `photo.png` sha256 字典序最小者（事前寫死，不得挑圖）。
  參數由使用者看圖＋該圖的設計尺寸表確認／修改（合成圖沒有「現場」）。程序 P 全部適用。新 `SHUFFLE_SEED`、新盲測編號。
  **作答選項與答案鍵等級**：作答表的五個選項逐字取 `SCENE_SPECS.json` 的 `category`——「浴室／客廳臥室／大空間／走廊樓梯間／車內」（每類只用一次；不得沿用 R2 的「教堂大空間」字樣，大空間類的盲測代表可能是教室）。`blind_test/MANIFEST.json` 含 `params_sha256`，可由鎖定 commit 反查類別，**與答案鍵同等級**：作答 commit 之前，使用者不得開啟，任何視窗不得向使用者轉述其內容。
  **REPORT §0 與 §1 強制句（逐字）**：「§7-1 held-out 五張為 AI 合成圖（GPT Image；Codex 2026-09-21 生成；使用者 2026-09-21 指定），非真實空間照片；尺寸為提示詞設計值、非量測；本結果不能外推為『對真實照片有效』——真實照片的 held-out 驗證在 T-17、T-17-R2 與本次都沒有做過；五題的尺寸與材質由使用者依設計表確認，本項不檢驗照片分析的任何能力。」
- **V2-2 RT60**：門檻＝500Hz、1kHz、2kHz、4kHz 逐頻段＋88.4–353.6 Hz 聯合帶（裁決 B 原字），皆 <20%，不變。
  **估計式**：參考側（真實 IR）與引擎側（生成 IR）一律用 R2 同一支 `src/image_reverb/ir_metrics.py`、同為 T30，呼叫寫死為 `band_t30(ir, fs, [500, 1000, 2000, 4000])` 與 `t30_low_combined(ir, fs)`；`ir`＝一維 float64 單聲道陣列（公開資料集的取法見 (d)；`mit_gym` 沿用 R2 既有程式碼路徑、不另寫），`fs`＝該檔**原生取樣率**（BUT 為 16 kHz）；**不得重取樣、裁切、濾波、正規化或做任何其他前處理**。R3 不得新寫或改用其他估計式（手機錄音不入名單後，原開放問題 A 的 T20 作法**撤回**）。 **警告不得升級為例外**：估計式的每一次呼叫（參考側、引擎側、(k)）都在 Python 預設的 warnings 過濾下執行——不得以 `-W`、`PYTHONWARNINGS`、`warnings.simplefilter`、`warnings.filterwarnings` 或任何其他方式把警告（如 `np.polyfit` 的 `RankWarning`）升級為例外或壓抑；偵測方式：每一支呼叫估計式的 R3 工具（(e)、(k) 的工具與引擎側量測工具）在程式開頭、**任何其他 `import` 之前**（只先 `import sys, warnings`；`import numpy`／`scipy` 會在 `warnings.filters` 加入它們自己的過濾項，所以必須在那之前記錄），以 `print(sys.warnoptions)` 與 `print(warnings.filters)` 把兩者逐字寫入該次執行紀錄（RR-5 的 `run_evidence/`；18-a(2)④ 的複驗者寫入其複驗紀錄）——前者須為 `[]`（`-W` 旗標與 `PYTHONWARNINGS` 環境變數都會反映在此）、後者須逐字等於同一 Python（`.venv`；`python -VV` 與 `env_python.txt` 逐字相同）下 `python -c 'import warnings; print(warnings.filters)'` 的輸出（R3 REPORT 與獨立複驗者各貼一次該指令輸出供比對）；另 R3 工具原始碼 `grep -n 'simplefilter\|filterwarnings' <每一支 R3 工具腳本>` 須為 0（R3 REPORT 貼指令與輸出，獨立複驗者重做）。 **不符合時的處置**：(甲) **事前自檢**——每一支上述工具在完成開頭紀錄之後、任何其他 `import` 之前、讀取任何輸入（封存檔、生成 IR、參數檔、結果檔）之前，自行比對兩件事：`sys.warnoptions == []`；以及**開頭紀錄那一刻取得的** `warnings.filters`（即前句規定在任何其他 `import` 之前取得並 `print` 的同一個值，不得事後重新取得）以 `print` 印出的那一行**含結尾換行**逐字等於 `run_evidence/warnings_filters_ref.txt` 的全部內容——等同 `str(該值) + '\n' ==` 檔案全文（該檔＝與 RR-3 環境指紋同批、在 R3 第一次執行任何呼叫估計式的工具之前，以同一 Python 執行上述 `python -c` 指令、將其 stdout 逐位元組存入；18-a(2)④ 的複驗者沿用 R3 commit 內的同一檔）；任一不符 → 以非 0 的 exit code 結束，不讀任何輸入、不寫任何輸出檔（開頭紀錄除外）。呼叫估計式的工具另在 `import numpy` 之後、讀取任何輸入之前，把 `np.geterr()` 的回傳值逐字寫入同一份執行紀錄，並比對其逐字等於 T-17-R3 卡事前所記、以同一 Python（`.venv`）取得的預設值（該卡在 R3 第一次執行任何呼叫估計式的工具之前記錄；18-a(2)④ 的複驗者沿用）；不符 → 同樣以非 0 的 exit code 結束，不讀任何輸入、不寫任何輸出檔（開頭紀錄與本項紀錄除外）。上述兩段自檢的結束都**不是**執行、也**不是**重跑：不計入 18-a(2) 的「各一次」、不計入 RR-5 的次數、不適用定義 RR；紀錄仍存 `run_evidence/` 並在 REPORT 逐次列出；修正啟動方式、環境或工具程式（改工具後須重做下述 grep，並把當次 HEAD 記入紀錄）後照常執行。原始碼 grep 須在每一支工具**第一次執行之前**做並把指令與輸出存入 `run_evidence/`；不為 0 → 該工具不得執行，改好後重做 grep。(乙) **事後發現**——已寫出結果檔的執行，其開頭紀錄不符合上述任一「須」、或 grep 事後不為 0 → 該次結果檔作廢、**不得重跑**，並依**該次執行算了什麼**分路（不依工具名稱；一次執行同時符合多路時，依下列順序取第一個符合的路）：18-a(2)④ 複驗者的執行 → 一律只走本路：該次複驗作廢，比照 18-a(2)④「不同＝複驗不通過」處理、不另觸發 P-3；其餘執行，只要對任何參考側（(d) 參考值計算或 (e) 可算性檢查）或引擎側（生成 IR）的 IR 呼叫了估計式——不論同一次執行是否兼算 (k) → 該次執行涵蓋的每一個場地依 P-3 處理，該次 (k) 結果（如有）一併作廢；只算了 (k)、沒有對任何參考側或引擎側的 IR 呼叫估計式的執行 → 該次 (k) 結果作廢、REPORT 記「(k) 作廢（K-1）」，依 (k) 本款不影響名單與判定。(甲)(乙) 並用；(乙) 不因 (甲) 未觸發而免除。
  **比較式**：每一判準頻段的誤差＝（引擎側 T30 − 參考值）÷ 參考值，以 float64 計算、不四捨五入；達成＝|誤差| **< 0.20（嚴格小於）**，恰為 0.20 記未達。**引擎側 T30**：一律為 R3 工具直接呼叫估計式（`band_t30`／`t30_low_combined`）對生成 IR 所得的 float64 原值，不四捨五入、不經 R2 `measure_file` 的 4 位小數路徑——`mit_gym` 的引擎側也是如此。 **引擎側拋錯**：引擎側與 (e) 相同，逐判準頻段各呼叫一次——四個逐頻段以 `band_t30(ir, fs, [f])`（`f` ∈{500, 1000, 2000, 4000}）各呼叫一次、聯合帶以 `t30_low_combined(ir, fs)` 呼叫一次（與估計式段所寫的四頻段一次呼叫逐位元相同，已於 (e) 說明）；引擎側估計式對某生成 IR 在某判準頻段拋出任何 Python 例外 → 屬 RR-2 類別者（程式判斷式與 (e) 同一條：`isinstance(e, MemoryError)`，或 `isinstance(e, OSError)` 且 `e.errno` ∈{5, 12, 28}；不得攔截、原樣重新拋出）依定義 RR 處理；其餘 → 該場地該頻段記**未達**（不得剔除、不得重跑、不得改呼叫方式或加任何前處理），例外類別與訊息原文逐筆入 REPORT。**參考值**：`mit_gym` 取 (e) 所定的 R2 `rt60_table.json` `real_reference` 原值（該檔存的是 4 位小數，照原值使用）；公開房間取 (d)(e) 未四捨五入的 float64。R2 程式 `scripts/t17_rt60_table.py` 的 `abs(err) <= 0.20` 與先四捨五入到 4 位小數是已知差異：不回溯改 R2、不用於 R3 判定；R3 工具卡照本條實作並在 REPORT 註明。這是收緊、不是放寬；門檻數字 20% 未動。
  - (a) **名單**＝`mit_gym`（U2）＋T-70 封存於 `assets/reference_rooms_heldout/` 的公開資料集房間中，同時符合下列五條件者：(i) 官方文件化尺寸最大維 ≤10 m（SPEC F-02 `GEOMETRY_SCOPE_MAX_M` 既有口徑）；(ii) 非 `domain_out_non_room`（T-55 v3／T-17-R2 §3 口徑：車內等不入名單）；
    (iii) 資料集官方附有 ≥1 張**合格照片**（不得以網路搜圖或合成圖替代）——合格＝同時符合：① 來源＝下列兩種之一，**且官方已標明屬該房間**（檔案位於該房間的官方目錄內，或圖號／檔名指明該房間；未標註者不收）：(A) 資料集本體內、位於該房間官方目錄下的**相機實拍 RGB 照片**——深度圖、3D 掃描貼圖、渲染圖、關節可視化、遮罩、頻譜圖等**不算**；判定＝(一) 副檔名（不分大小寫）∈{`.jpg`,`.jpeg`,`.png`,`.tif`,`.tiff`}（`.heic` 自清單刪除——`.venv` 的 PIL 未註冊 HEIC 解碼器，留著只會變成「打不開」的死分支）；(二) PIL `Image.open(...)` 開啟成功且 `.mode` ∈{`RGB`,`RGBA`}（只讀 `.mode`／`.size`，不 `load()`、不解碼像素、不 `save`；`Image.open` 拋出任何例外（含 `UnidentifiedImageError`）→ 不列候選、記入 `images_excluded`（理由：PIL 無法開啟），不得為此安裝或註冊額外解碼器）；(三) 檔名（含副檔名）與「該房間官方目錄（不含）以下、到該檔案（不含）為止的各層目錄名」各自以非英文字母字元（數字、`_`、`-`、`.`、空白等）切詞後**整詞**比對（不分大小寫），任一詞恰等於 `depth`、`scan`、`mesh`、`texture`、`render`、`mask`、`pose`、`joint`、`spec`、`spectrogram` 之一者排除（`perspective`、`exposure`、`scanner`、`inspection` 等只是含子字串者不排除）；通過 (一)(二)(三) 的**每一張**候選都交 T-70 的 Opus 驗證逐張人眼判定「是否相機實拍照片」（與 ③ 的人眼判定併為同一次），Opus 判非實拍者不收——沒有「無法判定才送」的觸發條件，執行者不得自行判定實拍與否；(B) **僅 SoundCam**：arXiv 2311.03517 PDF 內嵌的原生影像中，T-70 3a(3) 以人眼辨識為 Fig. 4（Treated Room 空房配置）與 Fig. 8（Conference Room）者（只准抽取 PDF 內嵌的原生影像，不得螢幕截圖、不得放大重取樣；`rooms.json` 記頁碼／圖號）。**不收**：BUT 官網頁的 11 張縮圖（未標註房間）、BUT 論文（Szöke et al. 2019）的插圖、其他任何網站／論文影像——T-70 3b(2) 清點的候選來源即上列 (A)(B)，兩處一致（T-70 r3-2、r4-2）；② PIL 讀得的長邊 ≥1024 px；③ 畫面中看得到地板與至少一面牆——T-70 執行者逐張記錄，**以 T-70 的 Opus 驗證逐張人眼判定為準**。多張合格 → 相對路徑依 (d)③ 同一比較方式最小者為 `--suggest` 輸入（相對路徑＝相對於該房間目錄 `assets/reference_rooms_heldout/<資料集>/<房間>/`、以 `/` 分隔的路徑字串，(A)(B) 同一根、(B) 即 `paper_figs/…`；依 Unicode 碼位逐字元比較，等同 Python `str` 預設排序、不做大小寫折疊、不做自然排序；只在合格者之間比，恰一張合格即該張）；T-70 3b 在 `rooms.json` 每個 `included` 房間記 `suggest_photo`＝{相對路徑, sha256}，經 T-70 的 Opus 驗證（自己重排一次）後寫死；P-1「參數檔照片指紋核對」與 T-17-R3 卡的照片路徑明列一律指該欄位。
    (iv) 有官方量測、官方已解卷積的全向麥克風 IR，且依 (d) 過濾後至少剩 1 支（不得自行由掃頻錄音解卷積）；(v) 授權允許本專案內部非商業使用。**符合者全數入名單、全數封存，不得挑選、不切開發集**（U4 精神；也避免「調參集驗收」，②-4）。T-64 的合成圖沒有真實 IR，**一律不入名單**（③-1 至此無矛盾）。
    **非方塊形房間**：官方標註為非方塊形的房間（例：BUT R112，L 形，官方拆成兩個方塊各記尺寸）**留在候選**；(i) 與 (c) 所用的尺寸＝「同時包住官方各方塊的最小軸對齊外接盒」，只能由官方檔案所載各方塊的尺寸與相對位置機械算出（算式與輸入值寫進 `rooms.json`；`gt_dims_source: dataset_documented_bounding_box`）；外接盒最大維 >10 m → 不符 (i)。官方檔案不足以唯一決定外接盒（例如只有各方塊尺寸、沒有相對位置）→ T-70 停下回 Fable，由使用者在任何 R3 量測前再決定；**不得**由執行者自行假設擺放方式。
    **R112 已知限制句（REPORT 逐字）**：「R112 官方標註為非方塊形；其**參考尺寸**（`gt_dims`，來源 `dataset_documented_bounding_box`）依使用者 2026-09-21 決定取兩方塊的最小軸對齊外接盒，體積大於實際房間，且 shoebox 模型無法表示 L 形；使用者確認的尺寸另見 V2-2(i) 雙欄——P-6 只『允許』照抄外接盒、不強制；Opus 與 Fable 曾建議移出名單。」不另加「確認值必須等於外接盒」的作廢規則（其他房間同樣允許確認值偏離官方值）；REPORT 的 V2-2(i) 表對 R112 另列「使用者確認值」與「外接盒值」兩組數字（報告項，不設門檻）。
  - (b) **`mit_gym` 參數**（U2；②-6）：尺寸寫死 9.00×6.00×2.90 m（REPORT 標 `gt_dims_source: opus_estimate_T17_table4`，「非公開真值」原字照列）；六面材質由使用者**只看** `site_photo_gym.png` 確認，依程序 P 鎖定。T-65 步驟 0 那組等價性測試值不得作為確認依據、確認過程不得向使用者展示該組值（使用者獨立確認後若巧合相同不算違規）。
    **已知限制句（REPORT 逐字）**：「`mit_gym` 的真實 RT60 與多組參數下的誤差已在 T-17／T-17-R2／T-58 公開，參數確認者可能看過這些結果。」
    **R3 前不得預演**：T-65 試用與任何 R3 前的流程演練**不得**使用名單場地與 (g)(h) 報告項場地的照片；R3 參數鎖定前，不得向使用者展示這些場地的任何參數檔內容、IR 或 `analysis.json`（含預測 RT60）。健身房照片在 R3 前只准 Sonnet／Opus 做 T-65 的 MD5 等價性比對（T-65 r2-1）。
  - (c) **公開資料集房間參數**：尺寸＝官方文件值（`gt_dims_source: dataset_documented`，附出處頁碼；非方塊形見 (a)）；六面材質由使用者看該房間的**全部合格官方照片**確認，**另可參照（照抄）官方材質文字**——僅限 T-70 依欄位白名單自官方說明檔（如 BUT `env_meta.txt`）抽出的 `materials_official.txt`（規則見 T-70 r2-3b；只含牆／地／天花板／家具等材質欄位）。官方說明檔**原檔**在參數鎖定前不得由使用者或任何協助確認的視窗開啟或轉述（可能含殘響類欄位，P-5）。沒有官方材質文字的房間（SoundCam、`mit_gym`）只看照片。官方材質名稱對映到 `data/materials.json` 的哪一個 id，由使用者決定。程序 P。**已知限制句**：「公開資料集的殘響時間可能已見於其論文；參數確認者聲明確認前未查閱。」
  - (d) **參考值與參考 IR 清單**：每房間每判準頻段的參考值＝該房間「參考 IR 清單」內各支 T30 的中位數。清單在 T-70 步驟 3b 依下列規則寫進 `ASSET_MANIFEST.json`，經 T-70 的 Opus 驗證後寫死：
    ① **「支」的定義**：一支＝**一個檔 × 一個麥克風聲道 × 一個量測位置（單一聲源—接收點對）**。單聲道 WAV＝一個檔一支；多聲道 WAV＝「一個檔 × 一個聲道」各一支。`.npy` 陣列＝依下列**位置軸規則**判定：(甲) **樣本軸**＝長度最大的軸（最大長度不唯一 → 停）；(乙) **聲道軸**：M＝官方文件所載麥克風數（官方文件未載麥克風數、或同一房間在官方文件中出現兩個以上不同的麥克風數——不論原因，含不同量測批次或不同陣列 → 停）；M＝1 → 不設聲道軸、聲道索引記 0（長度為 1 的軸依 (丙) 忽略，不論有幾個）；M≠1 → 其餘軸中長度等於 M 的那一軸（不唯一或不存在 → 停）；(丙) **位置軸**＝除樣本軸與聲道軸外所有長度 >1 的軸（可為 0 個、1 個或多個；長度＝1 的軸忽略）；位置索引＝各位置軸索引依軸序組成的元組；(丁) 一支＝（檔、聲道索引、位置索引），取法＝固定聲道索引與位置索引後沿樣本軸取出的一維陣列——取出後必須 `ndim == 1`（依 (甲)～(丙) 必然成立；若不成立＝規則本身失效 → 停）。執行者**不得**取第一個位置、取平均或全部展開；本款所有「停」（(甲) 最大長度不唯一、(乙) 官方文件未載麥克風數或出現兩個以上不同的麥克風數、(乙) 聲道軸不唯一或不存在、(丁) 取出非一維、下句的位置層級標示無法對到位置索引）一律回 Fable、走 (d)⑤，補寫時須同時定義 (d)③ 的排序鍵。空房過濾 (甲) 若在位置層級標示（官方 metadata 逐位置註記有人與否），依標示逐位置過濾；標示無法對到位置索引 → 停。只收官方已解卷積的 IR。
    ② **過濾（先濾後取）**：(甲) **空房**——只收量測時房內無人的 IR：(A) 官方以檔名／目錄／metadata 標示有人與否的資料集（SoundCam）→ 只收標示為空房者，標示方式記進 `rooms.json`；(B) 官方說明檔對「有人在場」隻字未提的資料集（預期 BUT）→ 全部視為空房——判定方式＝對該資料集的官方說明檔（BUT 為 `read_me.txt`）**與四個授權房間官方目錄內（遞迴）全部 `*.txt`**（副檔名不分大小寫；機械取法 `find <房間目錄> -type f -iname '*.txt'`）逐檔執行 `grep -ci` 逐字計數（只輸出數字、不輸出任何行）關鍵字 `person`、`people`、`human`、`occup`、`audience`、`listener`、`participant` 七個（`occup` 涵蓋 occupied／occupant／occupancy），每檔七個計數皆為 0 → 視為空房，`rooms.json` 記「官方說明檔未提及有人在場」＋逐檔檔名、sha256 與七個計數；任一檔任一計數 >0 → 停，回 Fable（走 (d)⑤；誤觸（如 `personal`）是安全方向，多走一輪 v2.1 即可）；(C) 其他情況（官方提及有人在場、但檔案層級無法區分）→ 停，回 Fable；(乙) **非 boxed**——BUT 只收麥克風 metadata 欄位 `SpeakerVisibility` 的值不是 `partly boxed`／`fully boxed` 者，欄位缺漏的麥克風不收；(丙) **配置**——SoundCam Treated Room **只用空房配置（無布幔隔板）**，照片只用該配置的官方實拍圖（論文 Fig. 4 或資料集內同配置影像）；布幔隔板配置的 IR 與照片（論文 Fig. 5）不使用、也不另算一間（官方尺寸只有一組，隔板配置沒有官方文件化的幾何與材質）；官方資料無法區分兩種配置 → 停，回 Fable。
    ③ **排序與上限**：過濾後依（相對路徑、位置索引元組、聲道索引）三元組排序：相對路徑＝相對於該房間官方目錄、以 `/` 分隔的路徑字串，依 Unicode 碼位逐字元比較（等同 Python `str` 預設排序；不做大小寫折疊、不做自然排序）；位置索引元組以整數逐元素比較（等同 Python `tuple[int, ...]` 預設排序：`(2,)` 排在 `(10,)` 之前，無位置軸的 `()` 排最前）；聲道索引為整數比較；整體等同 Python 對 `(str, tuple[int, ...], int)` 三元組的預設排序（`sorted(...)`）。超過 20 支取前 20 支；清單逐支記相對路徑、位置索引（無位置軸者記 `()`）、聲道索引、檔案 sha256。過濾後 0 支 → 該房間不符 (a)(iv)。
    ④ **允許的讀取方式**：T-70（封存期）只准讀 header——WAV 用 `soundfile.info`；`.npy` 用 `np.load(path, mmap_mode='r', allow_pickle=False)` 且只取 `.shape`／`.dtype`（不得索引、不得運算）；`.npy` 的取樣率取自官方文件並記出處，查不到 → 停。R3 參考值計算（鐵則 18-a(2)③）才讀樣本：WAV 用 `soundfile.read(path, dtype='float64', always_2d=True)` 取該聲道；`.npy` 用同一 `np.load` 取該聲道轉 float64。其他格式（HDF5、pickle 等）→ 停，回 Fable。
    ⑤凡「停，回 Fable」者：執行者不得當場解釋或挑選。Fable 的補充規則＝**對已核准判準的變更**（3b 的前置就是 `criteria:` commit，所以 3b 的「停」必然發生在判準寫入之後）：須另立新 `criteria_version`（`criteria: T-63 v2.1`、v2.2……）、獨立 commit、經獨立 Opus 審 diff、並早於任何 R3 量測（任何 `--params` 與 18-a(2)①②③ 的第一次執行之前）；補充規則不得出現或變動任何門檻數字（≥4/5、<20%、名單 ≥3、≤10 m、前 20 支、≥1024 px、重跑 ≤2 次）；每一次補充都用白話告知使用者，使用者可否決（WORKFLOW §7.4 允許由獨立審查者核准）。若「停」在 `criteria:` commit 之前就已由 3a 的目錄結構摘要預見，Fable 得在核准前的 r(n+1) 先補寫（照常經 Opus 審 diff），不必等到 v2.1。
  - (e) **名單兩段鎖定**（②-5、③-2）：第一段——候選名單在 T-70 經 Opus 驗證時寫死；第二段——參數鎖定 commit 之後、任何 `--params` 之前，執行「參考值可算性檢查」（只讀參考 IR、不讀任何生成 IR）：**逐支逐判準頻段呼叫估計式**——四個逐頻段以 `band_t30(ir, fs, [f])`（`f` ∈{500, 1000, 2000, 4000}）各呼叫一次、聯合帶以 `t30_low_combined(ir, fs)` 呼叫一次（與估計式段所寫的四頻段一次呼叫逐位元相同，因該函式逐頻段獨立濾波、獨立回歸）；**單支處置＝(乙) 剔除該支**——**拋錯＝該次估計式呼叫拋出任何 Python 例外**（含 `ir_metrics.py` 既有的四種 `ValueError`：頻段能量為 0、−5→−35 dB 區間少於 8 個樣本、斜率非負、`ndim ≠ 1`；含 `scipy` 自身的例外；不限類別）：其中屬 RR-2 類別者——**程式判斷式寫死**＝`isinstance(e, MemoryError)`（涵蓋 numpy 的 `MemoryError` 子類別），或 `isinstance(e, OSError)` 且 `e.errno` ∈{5, 12, 28}——R3 工具**不得攔截**，須原樣重新拋出（`raise`）使行程失敗，再依定義 RR 判定（重跑）；工具須在全部支、全部頻段算完後才一次寫出結果檔（失敗時不留半寫入檔，RR-1）；其餘例外攔截並一律依本條剔除——某支在某判準頻段拋錯 → 該支只在該頻段剔除（其他頻段照算），該房間該頻段的參考值＝其餘未拋錯各支 T30 的中位數；某房間某判準頻段**全部支**都拋錯 → 該房間出名單（不新增任何門檻）；剔除明細（房間、支、頻段、例外類別、例外訊息原文）逐筆列入 (e) 的 commit 與 REPORT。**`mit_gym` 怎麼套**：3 支＝3 個單聲道 32 kHz WAV 各一支；**參考值一律取 R2 `output/mvp_acceptance_r2/rt60_table.json` 內 `mit_gym` 的 `real_reference` 原值**（其產生路徑 `scripts/t17_rt60_table.py` 的 `measure_file`／`real_reference`——「拋錯頻段記 `None`、彙整時丟棄、跨檔取中位數」就是本條 (乙)，R2 時各頻段皆算得出）；R3 重算一次只作核對——**重算＝以 `import` 呼叫 `scripts/t17_rt60_table.py` 的 `measure_file`（對 3 個 WAV 各一次）與 `real_reference`（呼叫方式與該腳本 `main()` 相同：判準頻段＝`real_reference([m["bands"] for m in 三個結果], "<f>")`、聯合帶＝`real_reference(三個結果, "low_combined")`）**，結果寫 `output/mvp_acceptance_r3/reference_metrics/mit_gym_recheck.json`；gym 重算只作核對，沿用 R2 函式的內部例外處理（`measure_one_channel` 以 `except Exception` 包住估計式呼叫、拋錯頻段記 `None`），**不受 RR-2「不得攔截」之限**；重算值為 `None` 的頻段在核對紀錄照列（視為「與原值不同」，依下句如實列出、仍以原值為參考值）；**不得執行** `scripts/t17_rt60_table.py` 或 `scripts/t17r2_rt60_table.py` 的主程式（前者會覆寫不受版控的 `output/mvp_acceptance/rt60_table.json`＝T-17 第一次驗收證據，後者會覆寫鐵則 19 保護的 R2 產物）；**參考值的鍵路徑**＝R2 `rt60_table.json` 的 `venues[]` 中 `key == "mit_gym"` 那一筆的 `real_reference.bands."<f>".value`（`<f>` ∈{`500`,`1000`,`2000`,`4000`}）與 `real_reference.low_combined.value`（同層的 `min`／`max`／`n` 不是參考值；`value` 才是跨檔中位數，與 (d) 一致）；重算值與原值任一頻段不同 → 如實在 (e) 的 commit 與 REPORT 逐頻段列出兩組數值與差異，**仍以原值為參考值、不得改用重算值、不停**。選 (乙) 的理由（事前寫定、不看任何 R3 數值）：與 R2 既有程式行為一致、保住 v1 可比性；(甲)「任一支拋錯即整間出局」會讓單一支的雜訊決定名單、使「≥3」下限更脆弱。
    逐房間逐頻段數值與 pass／fail 獨立 commit。此後名單不得增刪；被刷掉的房間仍在 REPORT 列出並標理由。
    **(e) 如實註明**：本檢查只排除估計式拋出例外的支與頻段（主要是 `ir_metrics.py` 既有的三種 `ValueError`：頻段能量為 0、−5→−35 dB 區間少於 8 個樣本、斜率非負；另含 `ndim ≠ 1` 與 `scipy` 自身的例外），**不含雜訊底或動態範圍判斷**，預期幾乎所有檔案都會通過；它不是品質把關，REPORT 不得稱之為品質檢查。對應的揭露見 (k)。
  - (f) **判定式**（同 R2）：最終名單內**全部**場地五項皆達才算達成；最終名單 <3 個場地 → V2-2 記「未達（樣本不足）」，不得以 1～2 個場地過關。
  - (g) **必測反例 `racquetball_court_4`**（U3＝報告項；①-3a）：以輔助路徑跑一次——尺寸＝12.19×6.10×6.10 m（T-17 表 4 公開標準，逐字沿用）、六面材質由使用者只看照片確認、程序 P。REPORT 標「**原規格必測反例**（v2＝報告項；使用者 2026-09-21 決定）」，同列 v1 判定（R2 表 5 原值：0/5 ❌）與本次五項誤差；不計入 V2-2 判定。
    已知限制句：「T-17 的診斷 run `t17_diag_racquetball_hard` 已公開一組硬質材質下的結果，參數確認者可能看過。」
  - (h) **8 場地逐列表**（①-3b；WORKFLOW §7.6）：REPORT §0 必含一張表，欄＝場地｜v1 判定（取 R2 表 5 原值）｜是否在 v2 名單｜不在的理由（>10 m／非房間／無尺寸來源）｜輔助路徑五項誤差（報告項）。T-17 表 4 有尺寸的 5 個場地中，`mit_gym` 在名單；其餘 4 個（`mit_department_store`、`mit_restaurant`、`racquetball_court_4`、`steinman_hall`）以輔助路徑各跑一次當報告項
    （尺寸逐字沿用 T-17 表 4、材質使用者只看照片確認、程序 P）＝對應原「手動組另列成績」（裁決 C）。`cathedral_room`、`tunnel_to_hell`、`divorce_beach` 沒有任何尺寸來源且為天然洞窟／隧道／戶外 → 不跑輔助路徑，表中標「無尺寸來源；shoebox 模型不適用」，只列 v1 判定與 V2-D 的全自動報告項。
    **(h) 已知限制句（REPORT 逐字；比照 (g)）**：「`mit_department_store`、`mit_restaurant`、`steinman_hall` 的 R2 手動組結果（R2 REPORT 表 2）已公開，參數確認者可能看過。」
  - (i) **尺寸來源雙欄**（③-8）：REPORT 逐場地同時列程式欄位 `dims_source`（`params_confirmed`）與 `gt_dims_source`（`opus_estimate_T17_table4`＝gym／`dataset_documented`＝公開房間／`dataset_documented_bounding_box`＝官方標註非方塊形的公開房間／`T17_table4`＝其餘 4 個場地，並照抄表 4 對該列的來源原述／`prompt_design`＝合成圖）。
  - (j) **手機錄音**（U1）：不入名單。使用者日後若自行提供錄音，只能當報告項；要讓它入名單必須另立新 `criteria_version`，且早於任何 R3 量測。
    **(j) 標註**：此類報告項在 REPORT 須標「未經效度檢查（T-63 第一輪 Opus 意見 ②-8 各條未處理）」，不得當參考值引用。
  - (k) **參考 IR 品質揭露（報告項，不設門檻、不影響名單與判定）**：REPORT 逐房間逐判準頻段列 ① 參考值 T30 ÷「濾波器自身衰減時間」——**逐支計算**：以該支的樣本長度與 `fs` 建立單位脈衝（float64；第 0 個樣本＝1.0、其餘＝0），呼叫同一函式（`band_t30`／`t30_low_combined`）所得值為該支的濾波器衰減時間（函式拋錯記「不可算」）；房間值＝各支「該支 T30 ÷ 該支濾波器衰減時間」的中位數（不可算者剔除並列出）；② 每支參考 IR 每頻段「帶通後訊號的峰值平方」與「檔尾最後 10% 樣本的平均能量」的 dB 差（帶通＝`scipy.signal.sosfiltfilt` 配 `ir_metrics.py` 既有的 `_bandpass_sos`／`_low_combined_bandpass_sos`，與 `band_t30` 相同的零相位濾波；不另寫濾波器）。計算屬鐵則 18-a(2)③ 的同一次執行。
  - (l) **已知限制句（REPORT 逐字）**：「參考值本身有量測不確定度（估計 ±10% 量級；各資料集的取樣率、位元深度、雜訊底不同，BUT 為 16 kHz／16-bit），而門檻是 20%；V2-2 的達成／未達有一部分來自參考值的量測雜訊。小房間短殘響下，低頻帶濾波器自身的衰減可能拉長 T30（見 (k)①）。」
- **V2-3、V2-4**：同 SPEC §7-3、§7-4，不變。
- **V2-5 安全（門檻；三條全成立才達成）**
  - (a) `--suggest` 無副作用：執行前後，`output/` 全目錄（遞迴）除 `--out-params` 指定的那一個新檔外，檔案清單與 sha256 逐一相同；`git status --porcelain` 無其他新增。
  - (b) 未確認不出 IR：`dims_m.confirmed` 與六面 `confirmed` 任一**不是 JSON 布林 `true`**（字串 `"true"`、`1`、`null`、缺鍵皆算未確認）→ `--params` 必須 exit 3、列出未確認欄位、**不產生任何輸出檔**；四種繞法全擋：`--force-low-confidence`、刪掉 `confirmed` 鍵、`confirmed:"true"` 字串、只確認 `dims_m`。
  - (c) 域外標 attention——**受測集用外部標籤、事前鎖定，不由 gate 自己說了算**：① R2 §3 表的 7 個域外場地照片；② R2 合成圖 `heldout_hall`（out）、`heldout_car`（non_room）；③ T-64 路徑 S held-out 中類別＝車內、或設計尺寸最大維 >10 m 者。
    規則：每張受測照片的參數檔 `dims_m.attention==true` 且 `dims_m.attention_reasons` 非空；且（①②適用）`output/mvp_acceptance_r2/runs/<run>.log` 中標 `out_of_domain` 的每一面，其 `surfaces.<面>.attention==true` 且 `surfaces.<面>.attention_reasons` 非空（schema 由 T-65 r1 補）。
    **如實註明（REPORT 同樣逐字列出）**：①② 共 9 張中，只有 4 張有逐面檢查對象——由 `grep -n out_of_domain output/mvp_acceptance_r2/runs/<run>.log` 事前算出：`CathedralRoom`（south、ceiling）、`RacquetballCourt4`（ceiling）、`TunnelToHell`（floor）、`heldout_car`（west、east、south、north、ceiling）。其餘 5 張（`DivorceBeach`、`SteinmanHall`、`heldout_hall`、`site_photo_department_store`、`site_photo_restaurant`）在 R2 log 中沒有 `out_of_domain` 面，與 ③ 一樣只驗 dims 那一半；單張透視照的 `dims_m.attention` 依 T-65 規格一律 `true`，所以那一半是平凡成立——這 5 張與 ③ 的 (c) 等於無條件通過，其中包含 R2 REPORT §5 點名的百貨賣場「有信心的錯誤幾何」。本條不因此新增門檻（T-65 不得新增判斷）。
  - 執行者：(a)(b) 與 (c) 的 stub 版由 T-65 `test_params_flow.py`（T-65 r1 已補）；(c) 的實圖版由 T-17-R3 步驟 0 依上列受測集逐張檢查（①②的 `--suggest` 一律 `--out-params` 到新目錄，鐵則 19；③ 依鐵則 18-a）。
  - **報告項（不設門檻）**：held-out 逐張列建議值 vs 使用者確認值——尺寸各維是否在 ±30% 內、六面材質逐面異同、使用者原樣接受的欄位比例（取自 `analysis.json` 的 `fields_accepted`／`fields_edited`）。合成圖沒有量測 GT，本表只能說「建議與使用者確認值差多少」，不能說「建議準不準」。 **attention 標記率**：域內受測照片（held-out 中類別≠車內且設計尺寸最大維 ≤10 m 者、V2-2 候選名單各房間的 `--suggest` 輸入照片、`site_photo_gym.png`）逐張列 `--suggest` 參數檔中六面 `attention==true` 的面數／6 與 `dims_m.attention` 值，並列全體合計；資料取自 18-a(2)① 的同一份 `params_suggested/`，不另跑——`site_photo_gym.png` 不是封存檔、其 `--suggest` 不屬 18-a(2)①，但同樣只跑一次、輸出到同目錄 `params_suggested/mit_gym.json`（由 T-17-R3 步驟明列，與 V2-2(b) 的參數確認共用這一份）。目的＝揭露「把每一格都標 attention 以取得安全假象」的程度（對應 WORKFLOW §5.4.4 第 8 點的精神）；V2-5(c) 只檢查「該標的有沒有標」，本欄補「不該標的是不是也全標了」。
- **V2-6 素材可追溯**：held-out＝AI 合成圖 → 記生成工具、生成日、提示詞與 `GENERATION_PATHS.json`；公開資料集 → 記名稱、版本、網址、授權、引用文獻。`T-04 缺項` 附註規則照舊。
- **V2-D 永久揭露**：R3 REPORT §0 必列「v0.4 全自動路徑：T-17 FAIL、T-17-R2 FAIL（R2），永久保留」＋V2-2(h) 逐列表＋V2-2(g) 必測反例。R3 另對 held-out、8 場地、V2-2 名單的公開房間各跑一次**全自動路徑**當報告項（coverage、錯誤放行率，口徑＝裁定 T-57-D）；
  其中能以 v1 口徑機械算出的部分（§7-2 自動組分母／coverage、域外誤放數）照 v1 口徑算出並寫進 §0。
  **T-17-R3 的 §8 欄位寫法**：`verdict_under_original_criteria`＝「v1 受驗路徑（全自動）本卡只跑報告項、未作完整 v1 判定；v1 歷次判定＝T-17 FAIL、T-17-R2 FAIL（R2），見各卡」；`criteria_changed_after_first_result: yes`。
- **V2-W 合法字樣**：`PASS（R3；輔助路徑 v2；T-04 缺項；held-out＝AI 合成圖）`／`FAIL（R3）`。V2-2 樣本不足時字樣仍是 `FAIL（R3）`，另附「V2-2：未達（樣本不足）」，不得發明第三種字樣。
  一旦發生 V2-T 的門檻變更，PASS 只能寫 `PASS（R3；輔助路徑 v2；門檻於 R2 後由使用者變更；T-04 缺項；held-out＝AI 合成圖）`。不得寫無附註 PASS。
- **V2-T**：Fable 不提議放寬 <20%／≥4/5。使用者若要改，必須由使用者自己給數字、在任何 R3 量測前、走獨立 `criteria:` commit；Fable 只如實記錄「這是 R2 結果後、由使用者決定的門檻變更」，並觸發 V2-W 的強制字樣。
- **核准程序**：r5 → **獨立 Opus 審 diff**（r5 對 r4＋Opus r4 意見）→ 使用者核准 → Fable 開獨立 `criteria:` commit，**同一個 commit 同時寫入三處**：(1) SPEC §7＝本段（R5-2）條文；(2) WORKFLOW §5.4.4＝R5-2b 註記（原十點原字保留，只追加）；(3) TASKS.md Phase 1.10 鐵則段＝R5-2c 的 18-a（r5）條文（追加；r1 版 18-a 原字保留並標「由 r5 版取代」）。三處寫入的文字須與「Opus 審過＋使用者核准」的版本逐字相同（只機械刪除 `〔r2：…〕`、`〔r3：…〕`、`〔r4：…〕`、`〔r5：…〕`、`〔回應…〕` 標籤與行首縮排）；commit 訊息附核准版所在 commit hash 與核准者原話；Opus 事後以 `git diff` 比對三處。Opus 若再要求修改 → Fable 另起 r6 段（r5 保留），再審一次。

## 8. 已知風險

| 風險 | 影響 | 對策 |
|------|------|------|
| 單張照片的幾何/尺度歧義（照片看不出絕對大小） | RT60 估計失準 | 主路線改 metric depth 模型（T-05 已證相對深度不可用）；已知物件尺度（門 ~2.0m）降為**尺度校驗**用；手動尺寸覆寫升 P0 (F-09)。**T-11 評測結果（2026-08-25 定案）：metric depth 量程實證 ~20m 天花板，適用範圍明訂 ≤10m（範圍內浴室 +24% 達標）；範圍外強制 `confidence: low` ＋警示，出口為手動尺寸或環景。換更大模型（Metric-Indoor-Large）延後至 T-17 驗收後再評估** |
| **分析模型安靜地失敗**（高置信度輸出錯誤結果，Phase 0 已實證：車內全判成 wall、地毯判成 floor） | 使用者拿到錯的 IR 而不自知 | 信心值 gating + 分析報告明示警示；「封閉空間出現 sky 類別 = 模型在猜」防呆規則；人耳試聽驗收（§7 第 4 條）；手動覆寫 P0 |
| 照片視野外的空間（背後的牆）未知 | 幾何不完整 | **環景輸入於 Phase 1 前處理支援**（equirect→多視角投影），環景照可直接解決；一般透視照以空間類型統計先驗補全；影片輸入（F-20）為完整解 |
| 材質誤判（照片中的視覺材質 ≠ 聲學材質） | 音色偏差 | 兩階段辨識 + 信心 gating；材質手動修正升 P0；CLIP zero-shot 不夠時評估 MINC/DMS 類材質專用模型 |
| Python 管線與 C++ plugin 的整合複雜度 | Phase 2 延宕 | MVP 期間即驗證 local service 方案；或將模型轉 ONNX/CoreML 內嵌 |
| **複合場景的路徑串接是近似**（F-17）：忽略耦合房間的能量回饋、繞射細節、傳輸面積/位置的精確定位；路徑間相對音量是場景作者設定值 | 聽感與真實複合空間有偏差而數字看起來合理 | 輸出標 `method: "path_cascade_v1"` ＋近似聲明；TL 資料附出處與信心欄位；每次改動必過人耳試聽（§7-4）；完整耦合模擬留待 Phase 3 評估 |
| **全自動照片路徑兩次驗收未達**（T-17、T-17-R2）：材質辨識有信心時也錯（R2 放行照片可判面錯 8/10）、單張透視照尺寸系統性偏大且形狀比例固定、唯一 in-domain 對照場地被 gate 擋下 | 「丟照片就得到對的 IR」目前做不到 | **v0.5 轉向參數為主、照片為輔（F-18）**：照片只出建議值、使用者確認後才出 IR；建議品質另開改善軌——材質專用模型可行性（T-68）、幾何 v2（T-69，需真實照片＋實測尺寸 T-64）。兩次 FAIL 永久保留、R3 仍把全自動路徑當報告項量測（不得讓原失敗消失，WORKFLOW §7.6） |
| **文字場景無 ground truth**（F-16）：同一描述對應無限多真實空間 | 使用者預期與輸出不符 | preset 尺寸/材質全部可被顯式參數覆寫；輸出列出實際採用的 preset 與所有假設值；認不得就報錯不猜 |

## 9. 名詞

- **IR (Impulse Response)**：空間對脈衝聲的響應，完整描述該空間的線性聲學特徵
- **RT60**：聲音衰減 60dB 所需時間（殘響時間）
- **Convolution Reverb**：將輸入音訊與 IR 做卷積以重現空間聲學
- **Image-source method**：以鏡像聲源計算反射路徑的幾何聲學模擬法
- **FDN (Feedback Delay Network)**：演算法式殘響的常用結構
