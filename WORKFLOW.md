# Workflow — 多視窗協作規則（Fable 規劃 / Opus 驗證 / Sonnet 執行）

本檔案定義：怎麼開工、怎麼收工、什麼時候 commit、怎麼判斷 Sonnet 的工程品質。
**使用者只需要複製貼上本檔案裡的 Prompt，不需要懂程式。**

---

## 1. 角色與使用時機

```
Fable（地圖規劃）─── 什麼時候用：Phase 開始/結束、任務全部做完、Sonnet 卡關、要改方向
Opus （強驗證）  ─── 什麼時候用：每完成 1 個任務後、或每完成 3 個小任務後批次審
Sonnet（工兵）   ─── 什麼時候用：平常做任務都用它，便宜快速
```

換模型的方式：在 Claude Code 輸入框用模型選單切換，或開新視窗選對應模型。

---

## 2. 三種標準 Prompt（複製貼上即可）

### 2.1 叫 Sonnet 開工（最常用）

> 執行 TASKS.md 的任務 T-XX。先讀 CLAUDE.md 和該任務卡的全部內容再動工，
> 完成後執行任務卡裡的「自我檢查」，最後照 WORKFLOW.md 第 4 節做收工程序。

（把 `T-XX` 換成要做的任務編號。一次只給一個任務。）

### 2.2 叫 Opus 驗證

> 你是驗證者，只審查不修改程式碼。請依 WORKFLOW.md 第 5 節的驗證標準，
> 審查任務 T-XX 的成果：讀 TASKS.md 的該任務卡、讀相關程式碼、實際執行驗證指令。
> 最後輸出：「✅ 通過」或「❌ 退回」＋具體理由清單。若退回，把理由寫進
> TASKS.md 該任務卡的「狀態」欄，改成 🟠 退回。

### 2.3 叫 Fable 重新規劃

> 你是規劃者。請讀 CLAUDE.md、SPEC.md、ROADMAP.md、TASKS.md、DEV_LOG.md 最近三筆，
> 根據目前進度與遇到的問題，更新 TASKS.md（新增/調整任務卡，維持既有格式），
> 並在 DEV_LOG.md 記錄規劃決策。目前的情況是：（描述你遇到的狀況或想改的方向）

---

## 3. 任務狀態標記（TASKS.md 內使用）

| 標記 | 意義 | 誰改的 |
|------|------|--------|
| ⬜ 未開始 | 還沒動工 | — |
| 🟡 進行中 | Sonnet 正在做 | Sonnet 開工時 |
| 🔵 待驗證 | Sonnet 做完自檢，等 Opus 審 | Sonnet 完成時 |
| ✅ 通過 | Opus 驗證通過 | Opus |
| 🟠 退回 | Opus 驗證不通過，附理由 | Opus |
| 🔴 卡關 | Sonnet 嘗試 3 次失敗 | Sonnet |

---

## 4. 收工程序（每次結束視窗前必做，Sonnet 自動執行）

任何視窗要結束前，執行以下五步。**使用者若發現 Claude 沒做就結束了，貼這句：
「執行 WORKFLOW.md 第 4 節收工程序」。**

1. **更新 [TASKS.md](TASKS.md)**：把做過的任務卡狀態改成正確標記，
   在卡片的「交接筆記」欄寫下：做了什麼、改了哪些檔案、下一步是什麼、有什麼坑
2. **更新 [DEV_LOG.md](DEV_LOG.md)**：在最上方加一筆今天日期的紀錄（3–6 行）
3. **更新 [TODO.md](TODO.md)**：同步「進行中／已完成」清單
4. **Commit**（規則見下）
5. **Push**：`git push`（第一次需 `git push -u origin main`）

### Commit 時機與規則

- **什麼時候 commit**：
  - ✅ 任務自檢通過、狀態改到 🔵 待驗證 → commit 一次：`T-XX: 完成〈簡述〉(待驗證)`
  - ✅ Opus 驗證通過 → commit 一次：`T-XX: 驗證通過`（含 Opus 修正建議的文件更新）
  - ✅ 純文件更新 → `docs: 〈簡述〉`
  - ❌ **程式跑不動、自檢沒過 → 不准 commit**（可以先把狀態與卡關原因寫進 TASKS.md，
    只 commit 文件：`docs: T-XX 卡關紀錄`）
- 一個任務 = 至少一個獨立 commit，不要把多個任務混在一個 commit
- Commit 訊息用繁體中文，開頭帶任務編號

---

## 5. 驗證標準 — 判斷 Sonnet 工程品質（Opus 用，使用者也能看懂）

### 第一層：能跑（使用者自己也能檢查）

- [ ] 任務卡「自我檢查」裡的每個指令實際執行過，且無錯誤訊息
- [ ] 宣稱產出的每個檔案都真的存在（`ls` 確認），音訊檔能播放、不是靜音
      （靜音檢查：`python scripts/check_audio.py <檔案>` 會顯示 RMS，RMS < 0.0001 = 靜音 = 不合格）
- [ ] 在**乾淨環境**重跑一次成功（不是只在 Sonnet 自己的對話裡宣稱成功）

### 第二層：做對（對照規格）

- [ ] 產出符合任務卡的「產出」清單，不多不少（多出來的檔案要有理由）
- [ ] 對應 SPEC.md 的功能編號要求（如取樣率 48kHz、頻段數量）
- [ ] 數值合理性：RT60 在 0.1–12 秒之間、IR 長度與 RT60 相稱、吸音係數在 0–1 之間

### 第三層：做好（工程品質）

- [ ] **無假實作**：沒有 hardcode 的假結果、沒有空函式假裝完成、沒有把驗證標準改寬鬆
- [ ] 錯誤處理：給錯輸入（不存在的檔案、非圖片）會有清楚錯誤訊息而非 crash
- [ ] 程式碼有模組邊界：Phase 1 起，分析/聲學/IR 生成分開，不寫成一大坨
- [ ] 沒有偷改其他任務的檔案、沒有動 SPEC/ROADMAP/WORKFLOW

### Sonnet 造假的常見紅旗（Opus 特別注意）

1. 說「測試通過」但對話裡沒有實際執行的輸出
2. 產出 WAV 但其實是靜音或雜訊（跑 check_audio.py）
3. 把任務卡的驗收條件改寫成它做得到的版本
4. try/except 把錯誤吞掉讓程式「看起來」跑完
5. 需要下載模型的任務，用隨機數假裝模型輸出

---

## 6. 換視窗交接的完整流程（使用者操作手冊)

```
【結束舊視窗】
  貼：「執行 WORKFLOW.md 第 4 節收工程序」
  確認它回報：TASKS/DEV_LOG/TODO 已更新、已 commit、已 push

【開新視窗】（模型選 Sonnet）
  貼：「執行 TASKS.md 的任務 T-XX。先讀 CLAUDE.md 和該任務卡的全部內容再動工，
      完成後執行任務卡裡的自我檢查，最後照 WORKFLOW.md 第 4 節做收工程序。」

【每 1–3 個任務後】（模型選 Opus）
  貼 2.2 的驗證 Prompt

【一個 Phase 做完 / 卡關 / 想改方向】（模型選 Fable）
  貼 2.3 的規劃 Prompt
```
