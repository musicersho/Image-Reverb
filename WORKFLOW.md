# Workflow — 多視窗協作規則（Fable 規劃 / Opus 驗證 / Sonnet 執行）

本檔案定義：怎麼開工、怎麼收工、什麼時候 commit、怎麼判斷 Sonnet 的工程品質，
以及（2026-09-03 起）**四軸狀態模型與變更控制規則**。
**使用者只需要複製貼上本檔案裡的 Prompt，不需要懂程式。**

> 版本紀錄：v1 2026-08-16 建立（026fa69）。**v2 2026-09-03（裁決 T-45-A）**：
> 依外部稽核 `REVIEW_STANDARD_AUDIT_T00_T44.md` 修正——單一「✅ 通過」廢止，
> 改四軸狀態（§3）；新增變更控制（§7）與實驗卡不可變欄位（§8）；
> 驗收標準拆成工程／實驗／產品／MVP 四種（§5.4）。v1 的內容全部保留，只加不刪。

---

## 1. 角色與使用時機

```
Fable（地圖規劃）─── 什麼時候用：Phase 開始/結束、任務全部做完、Sonnet 卡關、要改方向、改驗收標準
Opus （強驗證）  ─── 什麼時候用：每完成 1 個任務後、或每完成 3 個小任務後批次審
Sonnet（工兵）   ─── 什麼時候用：平常做任務都用它，便宜快速
```

換模型的方式：在 Claude Code 輸入框用模型選單切換，或開新視窗選對應模型。

---

## 2. 三種標準 Prompt（複製貼上即可）

### 2.1 叫 Sonnet 開工（最常用）

> 執行 TASKS.md 的任務 T-XX。先讀 CLAUDE.md 和該任務卡的全部內容再動工，
> 完成後執行任務卡裡的「自我檢查」，最後照 WORKFLOW.md 第 4 節做收工程序。
> 收工時任務卡狀態一律寫四軸（WORKFLOW §3），不准只寫一個圖示。

（把 `T-XX` 換成要做的任務編號。一次只給一個任務。）

### 2.2 叫 Opus 驗證（2026-09-03 v2）

> 你是驗證者，只審查不修改程式碼。請依 WORKFLOW.md 第 5 節的驗證標準，
> 審查任務 T-XX 的成果：讀 TASKS.md 的該任務卡、讀相關程式碼、實際執行驗證指令。
> 最後輸出**四軸判定**（WORKFLOW §3）：
> 「工程：已驗證／退回｜實驗：正向／負向／不確定／不適用｜產品：預設啟用／feature flag／不採用／待裁決／不適用｜MVP：PASS／FAIL／待重驗／不適用」＋具體理由清單。
> **禁止只輸出「✅ 通過」。** 若工程退回，把理由寫進 TASKS.md 該任務卡的「狀態」欄。
> 若你認為任務卡的驗收門檻本身有錯，**不得**在驗證紀錄裡用附註豁免——
> 照 WORKFLOW §7 走變更控制（退回或交 Fable 開新版門檻）。

### 2.3 叫 Fable 重新規劃

> 你是規劃者。請讀 CLAUDE.md、SPEC.md、ROADMAP.md、TASKS.md、DEV_LOG.md 最近三筆，
> 根據目前進度與遇到的問題，更新 TASKS.md（新增/調整任務卡，維持既有格式），
> 並在 DEV_LOG.md 記錄規劃決策。目前的情況是：（描述你遇到的狀況或想改的方向）

---

## 3. 任務狀態標記（TASKS.md 內使用）——四軸模型（2026-09-03 v2）

### 3.1 為什麼要改（一句話）

舊模型只有一個「狀態」欄，同一個 `✅ 通過` 同時代表「程式做完」「實驗假設成立」
「產品採用」「MVP 達標」四件不同的事。T-39 正是「工程完成、實驗負向、產品不採用、
MVP 仍未通過」，壓成單一 ✅ 後資訊必然失真（稽核 P0）。

### 3.2 四個欄位（每張卡固定使用）

| 欄位 | 可用狀態 | 說明 | 誰改的 |
|---|---|---|---|
| `工程`（engineering_status） | 未開始／進行中／待審／已驗證／退回／卡關 | 程式、測試、報告與交付物是否按卡完成 | Sonnet（未開始→進行中→待審／卡關）；Opus（已驗證／退回） |
| `實驗`（experiment_outcome） | 正向／負向／不確定／不適用 | 假設是否被資料支持 | Opus（依 REPORT 判定） |
| `產品`（product_adoption） | 預設啟用／feature flag／不採用／待裁決／不適用 | 是否真的進入產品預設路徑 | Fable 裁決（Opus 只能建議） |
| `MVP`（mvp_status） | PASS／FAIL／待重驗／不適用 | **只能由完整 MVP gate（T-17 系列）決定** | Opus 主導的 MVP 驗收卡 |

**寫法（TASKS.md 卡片內固定格式）**：

> `- **四軸狀態**：工程：已驗證｜實驗：負向｜產品：不採用｜MVP：FAIL（沿用 T-17）`

**禁止只寫**：`✅ 通過`。

### 3.3 圖示的固定語義（保留舊圖示，但意義收窄）

| 圖示 | 只代表 | 不代表 |
|---|---|---|
| ⬜ 未開始／🟡 進行中／🔵 待審／🔴 卡關 | 工程軸的進度 | — |
| ✅ | **工程交付已驗證**（僅此） | 實驗成功、產品採用、MVP 通過 |
| 🟠 | 工程退回 | — |
| 🟢／🔴／🟡（放在「實驗」後） | 實驗正向／負向／不確定 | — |
| 🚀／🧪／🚫 | 產品：預設啟用／feature flag／不採用 | — |
| `MVP PASS`／`MVP FAIL` | 只能用文字，不得只用圖示 | — |
| 🔮 | Fable 裁決卡／裁決紀錄 | — |

### 3.4 回溯規則

- 2026-09-03 之前的卡片，原「狀態」欄**一字不改**（那是當時的 verdict），
  在其下方加一行「四軸狀態（裁決 T-45-A 回溯標記）」。
- 舊 verdict 不覆寫：原標準失敗就是失敗；若標準後來修正，只能新增
  「依新標準的第二個 verdict」（§7）。

---

## 4. 收工程序（每次結束視窗前必做，Sonnet 自動執行）

任何視窗要結束前，執行以下五步。**使用者若發現 Claude 沒做就結束了，貼這句：
「執行 WORKFLOW.md 第 4 節收工程序」。**

1. **更新 [TASKS.md](TASKS.md)**：把做過的任務卡狀態改成正確標記（**四軸**，§3），
   在卡片的「交接筆記」欄寫下：做了什麼、改了哪些檔案、下一步是什麼、有什麼坑
2. **更新 [DEV_LOG.md](DEV_LOG.md)**：在最上方加一筆今天日期的紀錄（3–6 行）
3. **更新 [TODO.md](TODO.md)**：同步「進行中／已完成」清單（狀態以 TASKS.md 四軸為單一事實來源）
4. **Commit**（規則見下）
5. **Push**：`git push`（第一次需 `git push -u origin main`）

### Commit 時機與規則

- **什麼時候 commit**：
  - ✅ 任務自檢通過、狀態改到 🔵 待審 → commit 一次：`T-XX: 完成〈簡述〉(待驗證)`
  - ✅ Opus 驗證工程通過 → commit 一次：`T-XX: 驗證通過（工程）`，訊息內附四軸判定
    （含 Opus 修正建議的文件更新）
  - ✅ 純文件更新 → `docs: 〈簡述〉`
  - ✅ **驗收門檻變更 → 獨立 commit**：`criteria: T-XX v〈版號〉〈簡述〉`（§7；不得與結果同 commit）
  - ❌ **程式跑不動、自檢沒過 → 不准 commit**（可以先把狀態與卡關原因寫進 TASKS.md，
    只 commit 文件：`docs: T-XX 卡關紀錄`）
- 一個任務 = 至少一個獨立 commit，不要把多個任務混在一個 commit
- Commit 訊息用繁體中文，開頭帶任務編號

---

## 5. 驗證標準 — 判斷 Sonnet 工程品質（Opus 用，使用者也能看懂）

### 第一層：能跑（使用者自己也能檢查）

- [ ] 任務卡「自我檢查」裡的每個指令實際執行過，且無錯誤訊息
- [ ] 宣稱產出的每個檔案都真的存在（`ls` 確認），音訊檔能播放、不是靜音
      （靜音檢查：`python scripts/check_audio.py <檔案>` 會顯示 RMS，RMS < 0.0001 = 靜音 = 不合格）
- [ ] 在**乾淨環境**重跑一次成功（不是只在 Sonnet 自己的對話裡宣稱成功）

### 第二層：做對（對照規格）

- [ ] 產出符合任務卡的「產出」清單，不多不少（多出來的檔案要有理由）
- [ ] 對應 SPEC.md 的功能編號要求（如取樣率 48kHz、頻段數量）
- [ ] 數值合理性：RT60 在 0.1–12 秒之間、IR 長度與 RT60 相稱、吸音係數在 0–1 之間

### 第三層：做好（工程品質）

- [ ] **無假實作**：沒有 hardcode 的假結果、沒有空函式假裝完成、沒有把驗證標準改寬鬆
- [ ] 錯誤處理：給錯輸入（不存在的檔案、非圖片）會有清楚錯誤訊息而非 crash
- [ ] 程式碼有模組邊界：Phase 1 起，分析/聲學/IR 生成分開，不寫成一大坨
- [ ] 沒有偷改其他任務的檔案、沒有動 SPEC/ROADMAP/WORKFLOW

### 第四層：四種驗收分開判（2026-09-03 v2）

#### 5.4.1 工程驗收（決定「工程」軸）

工程卡只有在以下**全部**成立時才能標 `工程：已驗證`：

- 任務卡所有自我檢查完成；**未完成項不得用備註豁免**（T-04／T-12 教訓）；
- 修 bug 類新測試在舊碼上實測 fail、在新碼上 pass（純補覆蓋率的用突變測試證明非空）；
- 完整測試套件（全部 `scripts/test_*.py`）exit 0；
- 產物、報告、原始表格互相一致（REPORT 摘要與程式產出的表不得矛盾——T-44 教訓）；
- 不得吞錯、不得用舊快取（T-40 指紋）、不得用手寫摘要取代程式結果（地雷 #15）；
- 檔案範圍與 frozen artifacts 未遭未授權修改；
- 若有未達項，只能退回、卡關或先走 §7 變更控制，**不能直接綠燈**。

#### 5.4.2 實驗有效性（決定「實驗」軸）

實驗即使結果為負，也可以 `工程：已驗證`，但還必須單獨判斷：

- PLAN、假設、主要指標、停止條件均在執行前鎖定（單獨 commit 早於首輪）；
- 調參集與驗證集分離；至少保留一組 held-out（13 張同時調參與驗收＝過擬合紅線）；
- 每輪資料、程式、模型、快取皆有 hash（T-40 指紋機制）；
- 報告同時列出所有輪次，不只挑最好結果；
- 負向結果標 `實驗：負向`，**不得顯示成產品成功**。

#### 5.4.3 產品採用（決定「產品」軸，Fable 裁決）

分兩級：

**研究性採用（🧪 feature flag）**：
- 只要求可重現、相對改善、無測試回歸；
- 只能放在 feature flag 下，預設關閉；不得改成預設路徑；
- CLI／JSON 明確標記 experimental。

**預設產品採用（🚀）**，至少同時通過：

1. 相對改善門檻（卡片事前寫死）；
2. **事前寫死的絕對品質下限**（數值不得由驗證者看到結果後決定；須另開使用者核准的規格決策卡在執行前鎖定）；
3. 各角色／各輸入域均有最低門檻，不能讓整體平均掩蓋 floor；
4. **新增放行的已知錯誤案例＝0**；
5. 任何由 BLOCK→pass 的案例，逐例通過 ground truth 或端到端聲學驗證；
6. 已知關鍵案例不得退步：浴室、臥室、壁球場、體育館、車內；
7. held-out 結果與調參集方向一致；
8. 對最終使用者輸出的 RT60／盲聽至少不退步，而不只是內部材質分數上升——
   **最終仍以端到端 T-17 系列 gate 為產品判準**。

#### 5.4.4 MVP 驗收（決定「MVP」軸）

保留 T-17 原本的核心硬標準，**不用 T-38／T-39／T-44 的工程完成狀態代替**：

- 盲聽配對 ≥4/5；
- 自動照片路徑在事前定義的 in-domain 場地，500Hz–4kHz 逐頻段與低頻聯合帶誤差皆 <20%（裁決 B）；
- 域外輸入不得自信輸出錯誤 IR，必須安全 BLOCK 並提供可操作出口；
- 手動尺寸／材質出口另列成績，不得混入自動組稀釋失敗（裁決 C）；
- 外部 convolution reverb 可正常載入；
- 使用者試聽無重大 artifact；
- 所有素材來源與授權可追溯；
- gate 不能靠「全部擋掉」取得安全假象：另外報告 in-domain coverage 與錯誤放行率；
- 正式重驗使用新盲測編號與 held-out 素材，不能重用使用者已知道答案的舊盲測；
- 首次 FAIL 永久保留（T-17）；重驗另開卡（T-17-R2），結果另存，不覆寫。

### Sonnet 造假的常見紅旗（Opus 特別注意）

1. 說「測試通過」但對話裡沒有實際執行的輸出
2. 產出 WAV 但其實是靜音或雜訊（跑 check_audio.py）
3. 把任務卡的驗收條件改寫成它做得到的版本
4. try/except 把錯誤吞掉讓程式「看起來」跑完
5. 需要下載模型的任務，用隨機數假裝模型輸出
6. **（v2 新增）REPORT 的文字摘要與程式產出的表格矛盾**（T-44 §7 型）
7. **（v2 新增）驗證紀錄用附註豁免未達的字面條件**（T-12 型）——這是驗證者紅旗，不是執行者紅旗

---

## 6. 換視窗交接的完整流程（使用者操作手冊)

```
【結束舊視窗】
  貼：「執行 WORKFLOW.md 第 4 節收工程序」
  確認它回報：TASKS/DEV_LOG/TODO 已更新（四軸）、已 commit、已 push

【開新視窗】（模型選 Sonnet）
  貼：「執行 TASKS.md 的任務 T-XX。先讀 CLAUDE.md 和該任務卡的全部內容再動工，
      完成後執行任務卡裡的自我檢查，最後照 WORKFLOW.md 第 4 節做收工程序。」

【每 1–3 個任務後】（模型選 Opus）
  貼 2.2 的驗證 Prompt（輸出四軸判定）

【一個 Phase 做完 / 卡關 / 想改方向 / 想改驗收標準】（模型選 Fable）
  貼 2.3 的規劃 Prompt
```

---

## 7. 變更控制——驗收標準在結果出來後怎麼改（2026-09-03 v2）

1. **結果出來後不得直接改同一版門檻。** 必須建立新 `criteria_version`、獨立 commit
   （`criteria: T-XX v〈版號〉…`）、寫明變更理由與核准者，再重新跑。
2. **`criteria_commit` 必須早於首次執行結果**；同一 commit 不得同時修改門檻與提交結果。
3. **舊 verdict 不覆寫**：結果後改標準時，原 verdict 保留為 `verdict_under_original_criteria`，
   新 verdict 另記 `verdict_under_current_criteria`（§8）。
4. **結果後的標準變更，須由使用者或獨立審查者核准**，不能由同一角色自行更改後自行接受
   （T-38 拆卡 a0fe24e 是 Fable 自改自批，稽核已記錄）。
5. 若舊量測方法被證明無效，舊結果標「invalid／inconclusive」，**不能直接改成 PASS**。
6. 變更適用域時，原域內失敗不得消失；顯示「原規格 FAIL、新規格域外」，
   並驗證域外出口是否真的安全（T-11 教訓）。
7. 未完成任務卡自檢時，`工程` 不得是「已驗證」；若條件本身有錯，先走本節。
8. 產品採用門檻不得只有相對改善（§5.4.3）；缺絕對下限、安全 guard、held-out 的卡
   最高只能到 🧪 feature flag。
9. 任務卡、`HANDOFF.md`、`TODO.md` 的狀態以 TASKS.md 四軸為**單一事實來源**；
   交接文件停在上一卡＝過期，開新視窗以 DEV_LOG 最新一筆為準。

---

## 8. 實驗卡的不可變欄位（2026-09-03 v2）

每張實驗卡（凡是「結果不可事前保證」的卡：模型實驗、量測、驗收）在卡片內固定附：

```text
criteria_version:                 門檻版本（v1、v2…；改版就加號，原版保留）
criteria_commit:                  鎖定門檻的 commit（必須早於首次結果）
criteria_locked_at:               鎖定日期
dataset_manifest_sha256:          資料集清單的 hash（照片／ground truth／對照 IR）
implementation_commit:            程式改動 commit
result_commit:                    結果／REPORT commit
reviewer:                         驗證者（模型＋日期＋commit）
verdict_under_original_criteria:  依首版門檻的判定（永久保留）
verdict_under_current_criteria:   依現行門檻的判定
criteria_changed_after_first_result: yes/no
change_record:                    改門檻的 commit／理由／核准者（無則寫「無」）
```

規則：舊卡由 Fable 回溯補建（2026-09-03 已補 T-11／T-12／T-17／T-38／T-38B／T-39／T-44）；
新開的實驗卡在開跑前由執行者填前四欄，其餘由驗證者填。欄位一經填寫不得刪改，只能追加。
