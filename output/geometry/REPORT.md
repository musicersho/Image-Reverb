# T-11 幾何估計評測報告

> 產生日期：2026-08-18（§0–§6，原始評測關卡）／2026-08-26（§8，決策補丁後複測）
> 模型：`depth-anything/Depth-Anything-V2-Metric-Indoor-Small-hf`｜重跑方式：見本檔 §7
>
> ⚠️ **§0–§6 是原始（補丁前）紀錄，保留不改。** Fable 已於 2026-08-25 依 §6 定案路線
> （縮小適用範圍＋量程規則），Sonnet 已執行決策補丁並複測，結果見 **§8**。
> §8 有一項自我檢查未通過（Steinman 防濫殺對照組），詳見 §8.4，狀態見 TASKS.md T-11 卡。

## 0. 結論（一句話）

**metric depth 路線在本專案的照片上達不到 T-08 訂的 ±30% 判準，T-11 判定 🔴 卡關，回報 Fable 重新決策。**
這是 T-08 決策一**設計內的評測關卡**（「metric 模型精度未驗證 → 先實測，不達標就停」），
不是實作失敗。程式碼本身可用、手動覆寫路徑（F-09）正常運作。

---

## 1. 判準（照抄 TASKS.md T-11 步驟 5，未改動）

| # | 判準 | 結果 |
|---|---|---|
| A | 一般室內（浴室、走廊近端）尺寸誤差 ≤ **±30%** | ❌ **未通過**（走廊 −57%） |
| B | 車內與超大空間允許數字不準，但**必須輸出 `confidence: low`**，不得給出自信的錯誤數字 | ✅ 通過 |

**A 未通過 → 整個關卡未通過。**

---

## 2. 逐場地誤差表（全部 9 張照片，沒有只挑好看的）

「實際」欄的數字來自 `assets/SOURCES.md` 與 HANDOFF §2 已記錄的現場資訊；
沒有可靠實測值的就寫「未知」，不編造。

| 場地 | 實際進深 | 估 L | 估 W | 估 H | 估體積 | **進深誤差** | confidence | 判準 |
|---|---|---|---|---|---|---|---|---|
| 浴室 `bathroom_tiled` | 2.5–3.5 m（取 3.0） | 3.72 | 4.30 | 4.27 | 68 m³ | **+24%** | medium | ✅ A 通過 |
| 飯店走廊 `corridor_hotel_carpet` | ~30 m | 12.79 | 14.76 | 14.69 | 2772 m³ | **−57%** | medium | ❌ **A 未通過** |
| SUV 車內 `car_interior_suv` | ~2 m | 7.22 | 8.34 | 5.53 | 332 m³ | **+261%** | **low** | ✅ B 通過 |
| 體育館 `arena_ntsu_linkou` | ~150 m | 3.33 | 3.85 | 2.40 | 31 m³ | **−98%** | **low** | ✅ B 通過 |
| 樓梯間 `stairwell_tiled` | 未知 | 6.28 | 7.25 | 9.08 | 413 m³ | — | medium | — |
| Livehouse `livehouse_riverside_ximen` | 未知 | 2.75 | 3.18 | 2.00 | 18 m³ | — | **low** | — |
| CGI 洞窟 `cgi_cavern_crowd_sophy` | 未知 | 5.86 | 6.76 | 4.22 | 167 m³ | — | **low** | — |
| AI 臥室 `bedroom_ai_generated` | 未知 | 5.39 | 6.23 | 3.89 | 131 m³ | — | medium | — |
| CGI 洞穴實驗室 `cgi_cave_lab_sophy` | 未知 | 4.09 | 4.73 | 2.95 | 57 m³ | — | medium | — |
| Steinman Hall（環景，6 視角） | 未知 | 17.45 | 21.48 | 6.90 | 2585 m³ | — | medium | — |

⚠️ **Livehouse 估出 18 m³**（≈ 2.75×3.18×2.0 m）。那是一個可以站幾十人的表演空間，
估出來比一間小臥室還小——這是模型錯得最離譜的一筆之一，靠人群遮蔽規則標到了 low。

---

## 3. 根因：這個模型的輸出上限只有 ~20 m

先排除「是不是我們用錯模型／單位」：

- metric 版與相對版對同一張圖的輸出**確實不同**（浴室：metric min/max = 1.86/3.95，
  相對版 = 0.65/6.05 且語意相反），確認拿到的是距離而非 disparity。
- 浴室 p50 = 2.94 m，對照實際 2.5–3.5 m 合理 → 單位確實是公尺。

再看模型在 9 張照片上的**最大預測距離**：

| 照片 | max 預測距離 | p95 |
|---|---|---|
| bathroom_tiled | 3.95 m | 3.72 m |
| corridor_hotel_carpet | **19.69 m** | 13.01 m |
| car_interior_suv | 9.75 m | 7.22 m |
| **arena_ntsu_linkou（實際 ~150 m）** | **3.61 m** | 3.44 m |
| stairwell_tiled | 7.12 m | 6.28 m |
| livehouse_riverside_ximen | 5.40 m | 3.05 m |
| cgi_cavern_crowd_sophy | 9.15 m | 6.33 m |
| bedroom_ai_generated | 6.05 m | 5.40 m |

**9 張照片的最大預測距離全部落在 3.6–19.7 m，從沒超過 ~20 m。**

兩個直接後果：

1. **超大空間結構上無法表達。** 體育館實際 ~150 m，模型全圖最遠只說 3.61 m。
   這不是公式調得不夠好——**任何公式都無法從 3.61 m 推出 150 m**。
2. **走廊（~30 m）超出模型量程。** p95 只有 13.0 m、全圖最大 19.7 m，
   所以 −57% 的誤差有很大一部分是量程天花板造成的，不是可調參數。
   （`Metric-Indoor` 版是在室內資料集上訓練的，量程本來就是室內尺度。）

---

## 4. 最危險的發現：體育館的錯誤在深度數字裡完全沒有訊號

體育館估出 31 m³（實際是一個 ~150 m 進深的空間），誤差 −98%，但它的深度統計**看起來毫無異常**：

- `clamped_far_ratio` = 0.0（沒有任何像素被遠端 clamp 掉）
- p5/p50/p95 = 2.56 / 2.89 / 3.33 m，分佈平順、沒有離群
- 沒有逼近 clamp 上限（上限 50 m，實際最大 3.61 m）

也就是說：**只看深度輸出，沒有任何辦法發現它錯了 98%。**
這正是 HANDOFF §2 洞二與地雷第 9 條記錄過的失敗類型
——「模型會安靜地輸出看似合理的錯誤結果」。這次是第三次遇到。

### 4.1 因此補了一層「場景線索」信心判定

深度自己看不出錯，但**分割看得出來**。已實作於
`geometry.apply_scene_cue_confidence()`，三條規則各有實測依據：

| 訊號 | 門檻 | 為什麼 | 實測 |
|---|---|---|---|
| 地板可見度 | < 2% | 看不到地面就無法建立空間範圍，深度只量到最近的遮蔽物 | arena 0.0%、cgi_cavern 0.0% vs bathroom 6.8%、corridor 21.7% |
| 人群佔比 | > 20% | 人是強遮蔽物，量到的是人不是牆 | cgi_cavern 53.0%、livehouse 36.0% |
| T-12 判定域外 | — | CLIP 說「這不是建築表面」→ 那也不是能用 ShoeBox 描述的空間 | SUV 車內 `__vehicle_interior` 0.735 |

第三條是跨模組訊號，且**只有它抓得到車內**：ADE20K 沒有任何車輛內裝類別
（HANDOFF §2 洞二），車內的地板可見度 2.7%、人群 0%，前兩條規則都不會觸發。

**驗證這三條規則沒有濫殺**（對照組必須維持 medium）：

| 案例 | 應為 | 實際 |
|---|---|---|
| SUV 車內 | low | ✅ low |
| 體育館 | low | ✅ low |
| 浴室（誤差 +24%，判準內） | medium | ✅ medium |
| 走廊 | medium | ✅ medium |

判準 B 因此通過。**但判準 A 是精度問題，任何信心規則都救不了。**

---

## 5. 環景路徑（T-10 投影的副產品）明顯比單張照片合理

環景走的是不同幾何：四個水平視角各取中央中位數深度＝該方向牆距，**對牆相加**才是全長。
這是環景真正的價值——沒有「視野外」問題（提前解掉 SPEC §8 風險）。

Steinman Hall（唯一進得了 git 的環景素材）：17.45×21.48×6.90 m = 2585 m³，
四面牆距分別可查 `output/geometry/SteinmanHall/geometry.json` 的 `wall_distances_m`。
音樂廳量級合理，但**沒有實際尺寸可對照，所以不能拿來當判準 A 的證據**。

⚠️ 8 個對照場地有 5 張環景，但照片授權不允許再散布、沒進 git，
**5 張只有 1 張測得到**——這個驗證缺口與 T-10 遇到的是同一個（HANDOFF 地雷 #11）。

---

## 6. 給 Fable 的決策材料

### 已經確定的事實（不是推測）

1. metric-indoor-**Small** 的有效量程約 0–20 m，超大空間結構上無法表達。
2. 在量程內的小房間（浴室 3 m 級）誤差 +24%，**在 ±30% 內**。
3. 走廊（30 m）−57%、車內 +261%、體育館 −98%，全部超出判準。
4. 深度統計本身**無法**偵測自己的失敗；能偵測的是分割線索（已實作三條規則）。
5. 手動覆寫（F-09 / `--override-dims`）運作正常，是目前唯一可靠的尺寸來源。

### 可能的方向（Fable 決策，本卡不自行選擇）

| 方向 | 說明 | 成本／風險 |
|---|---|---|
| a. 換大模型 | `Metric-Indoor-**Large**`（~1.3 GB）或 `Metric-Outdoor-*`（量程較大） | 需使用者授權下載；**量程問題可能仍在**，Indoor 版的天花板是訓練資料決定的 |
| b. 縮小適用範圍 | SPEC 明確限定「一般室內小空間（≲10 m）」，超出就要求手動輸入 | 誠實、立刻可行；但 SPEC §7 驗收的 8 個場地有一半會落在範圍外 |
| c. 手動尺寸升為主路線 | 深度只用來給初始猜測，尺寸以使用者輸入為準 | 已經可用（F-09 已實作）；但偏離「照片自動分析」的專案願景 |
| d. 改用環景為主 | 環景對牆相加的幾何明顯較好 | 5 張環景只有 1 張測得到，**證據不足**，且使用者未必有 360 相機 |
| e. 放寬判準 | 把 ±30% 改寬 | ⚠️ **不建議**：±30% 是 SPEC F-02 的誤差目標，且 RT60 ∝ 體積，尺寸誤差會被平方/立方級放大（HANDOFF §2 洞一） |

⚠️ 特別提醒 e：本卡**沒有**為了通過而放寬任何判準。`config.GEOMETRY_ERROR_TOLERANCE`
仍是 0.30，程式與本報告都以它為準。

---

## 7. 重跑方式

```bash
cd "/Users/musicersho/Image Reverb"
source .venv/bin/activate

# 單張：前處理 + 幾何 + 材質
python -m src.image_reverb assets/photos/bathroom_tiled.png --geometry --materials-detect

# 手動覆寫（不跑深度模型，dims_source=manual）
python -m src.image_reverb assets/photos/bathroom_tiled.png --override-dims 4x3x2.5

# 環景（六視角對牆相加）
python -m src.image_reverb assets/reference_irs/steinman_hall/SteinmanHall.jpg --geometry
```

逐場地 JSON 在 `output/geometry/<照片名>/geometry.json`（含 `depth_stats`、`notes`、`scene_cues`）。
`output/` 只有 `*.md` 進 git，其餘需重跑產生。

---

## 8. 決策後複測（T-11 決策補丁步驟 8，2026-08-26）

Fable 2026-08-25 定案路線（見 TASKS.md T-11 卡「Fable 路線決策」）：自動幾何適用範圍
明訂「一般室內、估計最大尺寸 ≤ 10m」（`config.GEOMETRY_SCOPE_MAX_M`）。範圍外不是失敗，
是正式行為分支——`confidence: low` ＋警示，出口是 `--override-dims` 或環景輸入。
本節用**修訂後判準**重跑全部 9 張照片＋Steinman 環景，數字可用 §7 的指令重現。

`GEOMETRY_ERROR_TOLERANCE`（0.30）與 `CLIP_CONFIDENCE_THRESHOLD`（0.4）**未被動過**，
量程規則只加了新的一條、與既有三條場景線索規則（地板可見度／人群佔比／T-12 域外判定）
並存，兩者都只會把 confidence 往下修，取最嚴。

### 8.1 修訂後判準（照抄 TASKS.md T-11 卡步驟 8，未改動）

| # | 判準 | 結果 |
|---|---|---|
| A' | 範圍內（浴室）進深誤差 ≤ ±30% | ✅ **+24%**（不變，因為浴室在範圍內，量程規則未觸發） |
| B' | 範圍外（走廊、車內、體育館）**全部**必須 `confidence: low` | ✅ **三個全部 low**（走廊由 medium 翻 low） |
| 對照組 | 浴室維持 medium、Steinman 維持 medium（防濫殺） | ⚠️ **浴室 ✅ medium／Steinman ❌ 翻成 low**（見 §8.4） |

### 8.2 逐場地誤差表（補丁後，全部 9 張＋Steinman，沒有只挑好看的）

| 場地 | 實際進深 | 估 L | 估 W | 估 H | 估體積 | 進深誤差 | confidence（補丁前→後） | 判準 |
|---|---|---|---|---|---|---|---|---|
| 浴室 `bathroom_tiled` | 2.5–3.5 m（取 3.0） | 3.72 | 4.30 | 4.27 | 68 m³ | **+24%** | medium → medium | ✅ A' 通過、對照組通過 |
| 飯店走廊 `corridor_hotel_carpet` | ~30 m | 12.79 | 14.76 | 14.69 | 2772 m³ | **−57%** | medium → **low** | ✅ B' 通過（量程規則觸發：三維都 >10m） |
| SUV 車內 `car_interior_suv` | ~2 m | 7.22 | 8.34 | 5.53 | 332 m³ | **+261%** | low → low | ✅ B' 通過（沿用域外規則，未觸發量程規則） |
| 體育館 `arena_ntsu_linkou` | ~150 m | 3.33 | 3.85 | 2.40 | 31 m³ | **−98%** | low → low | ✅ B' 通過（沿用地板可見度規則，未觸發量程規則） |
| 樓梯間 `stairwell_tiled` | 未知 | 6.28 | 7.25 | 9.08 | 413 m³ | — | medium → medium | 不變（全部 <10m） |
| Livehouse `livehouse_riverside_ximen` | 未知 | 2.75 | 3.18 | 2.00 | 17 m³ | — | low → low | 不變（人群規則） |
| CGI 洞窟 `cgi_cavern_crowd_sophy` | 未知 | 5.86 | 6.76 | 4.23 | 167 m³ | — | low → low | 不變（地板＋人群規則） |
| AI 臥室 `bedroom_ai_generated` | 未知 | 5.39 | 6.23 | 3.89 | 131 m³ | — | medium → medium | 不變（全部 <10m） |
| CGI 洞穴實驗室 `cgi_cave_lab_sophy` | 未知 | 4.10 | 4.73 | 2.95 | 57 m³ | — | medium → medium | 不變（全部 <10m） |
| Steinman Hall（環景，6 視角） | 未知 | 17.45 | 21.48 | 6.90 | 2585 m³ | — | **medium → low** | ⚠️ 對照組未通過（見 §8.4） |

**A' 通過、B' 通過。** 走廊如預期由 medium 翻成 low——不是靠 hardcode 場地名，
是量程規則本身觸發（估出的 12.79/14.76/14.69 m 三維都超過 10m 門檻）。
車內與體育館維持 low，靠的是既有的域外判定／地板可見度規則（它們的估值本身 <10m，
量程規則沒有觸發，但既有規則已經抓到，符合「取最嚴」的設計）。

### 8.3 走廊翻 low 的觸發依據（Opus 驗證重點對應）

```json
"超出已驗證量程（適用範圍 ≤10m；length_m=12.8m、width_m=14.8m、height_m=14.7m）——
metric depth 模型實證天花板約 20m，且量程壓縮在天花板之前就開始（走廊實際 30m 被壓成
12.8m），超過此門檻的估值無法區分「真的 10–20m」與「被壓縮的 30m+」，數字不可信 →
confidence: low。建議用 --override-dims 指定實際尺寸，或改用 360° 環景照片輸入。"
```

這條訊息由 `geometry.apply_scope_confidence()` 依估出的**數值**（12.8/14.8/14.7m）
產生，程式裡沒有任何地方比對照片檔名或路徑——換一張任何估出 >10m 的照片都會觸發同一條規則。

### 8.4 ⚠️ 未通過的自我檢查：Steinman 防濫殺對照組

TASKS.md T-11 卡步驟 8 明文要求：「對照組防濫殺：浴室與 Steinman 必須維持 medium。」
浴室通過，**Steinman 沒有通過**——補丁後翻成 `low`。如實記錄，沒有調整規則或門檻讓它通過。

**根因**：環景的量程規則檢查的是六個視角**各自的原始牆距**（`wall_distances_m`），
不是配對相加後的 `length_m`/`width_m`。Steinman 的六個視角原始牆距：

| 視角 | 牆距 | 是否 >10m |
|---|---|---|
| az000_el00 | 12.199 m | ⚠️ 超過 |
| az090_el00 | 10.358 m | ⚠️ 超過 |
| az180_el00 | 5.254 m | 未超過 |
| az270_el00 | 11.125 m | ⚠️ 超過 |
| el+45（上） | 4.933 m | 未超過 |
| el-45（下） | 4.817 m | 未超過 |

四個水平視角有三個原始牆距超過 10m 門檻（相機明顯偏離廳中心：az180 方向只有
5.25m，對面 az000 方向卻有 12.2m）。**決策文字裡「17.45×21.48m，單面牆距未超標」
這句話用的是總長除以二的粗估（17.45/2≈8.7、21.48/2≈10.7），沒有對照
`output/geometry/SteinmanHall/geometry.json` 裡已經存在的實際六視角原始牆距**
——這份 JSON 在補丁前（2026-08-18）就已產生，本次只是重新驗證同一批數字，
不是新誤差。這不是本卡程式的臭蟲，是決策文字的估算與實際單視角數據不一致。

**沒有為了通過而調整**：量程規則的判定對象維持「單面牆距」（不是相加後總長），
這是決策文字自己給的理由（相加會讓有效上限拉高到約 40m，會漏掉真正超量程的單面牆）
——照著這個理由做，Steinman 就是會觸發，因為它確實有 3/4 面原始牆距超標。
換句話說，**這次翻 low 可能才是規則設計本身該有的行為**（相機明顯偏心、
單面牆距確實摸到模型量程邊緣，繼续標 medium 反而是那種「安靜地輸出看似合理的
錯誤結果」）；但這與任務卡白紙黑字的驗收語句衝突，**不是 Sonnet 能單方面認定
「這樣才對」就跳過的事**，需要 Fable 確認：(a) 接受 Steinman 翻 low
（環景本來就沒有 ground truth，維持一致的信心邏輯優先）、或
(b) 調整環景量程規則的判定方式（例如改成配對後的軸長，或提高環景專用門檻）。

**不影響 A'/B' 的結論**：A'（浴室 ±30%）與 B'（走廊/車內/體育館全部 low）
都是決策文字裡點名的核心通過條件，兩者皆通過；Steinman 只是輔助的防濫殺對照組，
且環景路徑本來就「無 ground truth，留待 T-17 用 8 場地真實 IR 間接檢驗」
（§5、決策文字皆已如實保留這個證據缺口）。
