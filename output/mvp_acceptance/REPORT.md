# MVP 驗收報告（T-17 / SPEC §7）

- **執行者**：Opus（T-17 卡明列「Opus 主導」）
- **執行日期**：2026-08-30
- **程式版本**：`git 61c448d`（T-18 驗證通過後的 main，`ir_metrics.py` 未再改動）
- **使用者回饋日期**：2026-08-30（§7-1 盲聽作答、§7-3 載入測試、§7-4 試聽皆已完成）
- **卡片狀態上限**：🔵 —— 依 **Fable 裁決 E**，照片來源網址未補齊前不得結案。
  四項標準已全部有結果，**§7-1 與 §7-2 未達標**。

---

## 0. 結論摘要

| SPEC §7 標準 | 判準 | 結果 |
|---|---|---|
| §7-1 盲聽配對 | ≥ 4/5 | ❌ **未達標，2/5**（使用者 2026-08-30 作答，見 §1） |
| §7-2 RT60 對照 | 500Hz–4kHz 逐頻段 ＋ 低頻聯合帶皆 <20% | ❌ **未達標**（自動組 0/8 場地、手動組 0/5 場地全達標） |
| §7-3 外部相容性 | 可載入任一 convolution reverb | ✅ **通過**（使用者實測：可載入、有殘響，見 §3） |
| §7-4 人耳試聽 | 記錄聽感與 artifact | ✅ **已執行並記錄**（無「鐵筒子」artifact；聽感指出殘響普遍過長，見 §4） |

**四項標準：1 通過、1 已執行、2 未達標。**

**一句話結論**：§7-2 明確未達標，且**病因已被隔離出來——不在幾何、也不在 IR 合成引擎，
而在材質辨識（CLIP）**。決定性證據是必測反例壁球場：自動路徑 −50%、換成官方標準尺寸
反而更差（−61%），但**只要把材質改對，同一支引擎立刻從 −50% 翻成 +13%（低頻聯合帶）**。
詳見 §2.4。

**§7-1 的盲聽獨立撞出一批缺陷，但病因不是同一個**：使用者把體育館聽成車內、把臥室
聽成教堂——**大小被聽反了**。查證後，使用者的耳朵每一題都正確描述了聽到的聲音，
**錯的是管線**。但三個錯題的根因**各不相同**：sample_1 是深度模型量程（幾何）、
sample_2 是域外輸入（車內不能用 ShoeBox 描述）、只有 sample_4 是材質。
**§7-1 不是 §7-2「病因在材質」的第二個證據，它是一組獨立的缺陷。** 詳見 §1.2。

> ⚠️ **本報告 2026-08-30 首版的修正**：首版在 §0 與 §6 把 §7-1 概括成
> 「與 §7-2 撞出同一個病因（材質）」。**這個概括不精確**——§1.2 的逐案歸因本身是對的
> （幾何／域外／材質各一），但摘要層把三個不同根因壓成一個。已更正。
§7-4 的聽感回饋「材質改對的版本殘響還應該再少 1–1.5 秒」也與量測一致
（該 run 1kHz 量測 5.085s vs 真實 2.926s，超出 2.16s）——**使用者的耳朵獨立佐證了量測**。

---

## 1. §7-1 盲聽配對 —— ❌ 未達標（2/5，目標 ≥4/5）

### 1.1 作答與對答

使用者於 2026-08-30 盲聽作答（未先看答案鍵）。

| 檔案 | 使用者聽成 | 正確答案 | 來源照片 | 判定 |
|---|---|---|---|---|
| `sample_1` | 車內 | **教堂／大空間** | `arena_ntsu_linkou.png` | ❌ |
| `sample_2` | 客廳臥室 | **車內** | `car_interior_suv.png` | ❌ |
| `sample_3` | 走廊樓梯間 | 走廊／樓梯間 | `stairwell_tiled.png` | ✅ |
| `sample_4` | 教堂大空間 | **客廳／臥室** | `bedroom_ai_generated.png` | ❌ |
| `sample_5` | 浴室 | 浴室 | `bathroom_tiled.png` | ✅ |

**得分 2/5**，目標 ≥4/5 → **未達標**。
參考基準：5 選 5 的強制一對一配對，**隨機亂猜的期望正確數是 1.0**——2/5 只比亂猜好一點。

### 1.2 🔬 關鍵：不是使用者聽錯，是管線把空間大小做反了

錯誤不是隨機分布的。**體育館 ↔ 車內、臥室 ↔ 教堂——大的被聽成小的、小的被聽成大的。**
量測這五條 IR 之後，原因非常清楚：

| 檔案 | 真正是 | 使用者聽成 | 管線估出的尺寸 | 估出體積 | 1kHz T30 | conf |
|---|---|---|---|---|---|---|
| `sample_1` | **體育館**（實際 ~150m 跨距） | 車內 | 3.3×3.8×2.4 m | **30.8 m³** | **0.324s** | low |
| `sample_2` | **SUV 車內**（實際 ~3–4 m³） | 客廳臥室 | 7.2×8.3×5.5 m | **332.4 m³** | 1.048s | low |
| `sample_4` | **臥室** | 教堂大空間 | 5.4×6.2×3.9 m | 130.7 m³ | **3.558s** | **medium** |
| `sample_3` | 樓梯間 | 樓梯間 ✅ | 6.3×7.2×9.1 m | 413.3 m³ | 4.439s | medium |
| `sample_5` | 浴室 | 浴室 ✅ | 3.7×4.3×4.3 m | 68.3 m³ | 3.043s | medium |

**使用者的耳朵每一題都是對的**——`sample_1` 聽起來確實像車內（0.324 秒殘響、30.8 m³），
`sample_4` 聽起來確實像教堂（3.56 秒殘響）。**錯的是管線，不是聽者。**
體育館被估成 30.8 m³（差了三個數量級）、車內被估成 332 m³（差約 100 倍）。

這正是 SPEC §7-4 那句「數字合理 ≠ 聽起來對」的反向版本：
**盲聽用完全獨立於 §7-2 的方式，撞出了同一批缺陷。**

**逐案歸因**：

- **`sample_1` 體育館 → 30.8 m³**：地雷 #12 的教科書案例。`metric_depth` 量程 ~20m 天花板，
  體育館 ~150m。管線**有**正確攔截並標 `low`＋警示原文
  「地板可見度只有 0.0%（門檻 2%）……估出的尺寸是**下限**而非實際房間大小」——
  **防呆規則有作用，但產品仍然輸出了一條聽起來像車內的 IR。**
  → 偵測到「不可信」之後只降信心、照樣出貨，對使用者的實際保護是零。
- **`sample_2` 車內 → 332 m³**：管線**正確認出**這不是建築空間，警示原文
  「T-12 的材質分類判定這不是建築表面（vehicle_interior）——這不是一個可以用 ShoeBox
  房間模型描述的空間，尺寸數字不可信」。地雷 #13 的域外選項在這裡**確實生效了**。
  但同樣地：認出來了，還是出貨了。
- **`sample_4` 臥室 → 3.56 秒殘響（🔴 最危險的一筆）**：這是**唯一沒有被攔下來的**——
  `confidence: medium`，沒有任何一條警示說空間或材質有問題。
  六個面是 `generic_wall`×4 ＋ `gypsum_board`×2，**1kHz 吸音係數只有 0.03–0.04**。
  但查 `surfaces_sources` 後要說得更準確：**四面牆的 `generic_wall` 是 CLIP 正常判定的
  結果（`source: clip`，不是 fallback）**——`generic_wall` 的提示詞是
  「a plain smooth plastered wall」，對一面臥室牆而言**視覺上判得沒錯**。
  只有地板是 fallback（CLIP 機率 0.24 → `gypsum_board`）。
  → **真正的病因不是「牆判錯」，是模型裡根本沒有床、棉被、窗簾、地毯這些吸音體**
  （α 0.37–0.72）。ShoeBox 六面模型只描述得了六個表面，**房間裡的東西無處可放**。
  真實臥室殘響應在 0.4 秒量級。
  **這與 §2.4 壁球場的「CLIP 把牆判成窗簾」是不同的病因**——那邊是判錯，這邊是判對了
  但模型表達不了室內陳設。

### 1.3 ⚠️ 機制觀察：fallback 材質是全表最不吸音的一群（但相關性證據不足）

`sample_4` 的地板 CLIP top-1 機率只有 0.24（低於 0.4 門檻）→ fallback。
查 `data/materials.json` 的 1kHz 吸音係數：

| 材質 | 1kHz α | | 材質 | 1kHz α |
|---|---|---|---|---|
| `marble` | 0.01 | | `carpet` | 0.37 |
| `concrete` | 0.02 | | `grass_soil` | 0.69 |
| `generic_wall` | 0.03 | | `curtain_fabric` | 0.72 |
| **`gypsum_board`（實際 fallback）** | **0.04** | | `acoustic_panel` | 0.85 |
| `brick` | 0.04 | | `audience_seating` | 0.96 |

**實際 fallback 值是 `gypsum_board`**（1kHz α=0.04，12 種裡第 4 不吸音）。
「模型不知道」時的預設，在聲學上接近「假設這個面不吸音」，方向上會把殘響推長。

> ⚠️ **`generic_wall` 不是 fallback，是 CLIP 的正常候選之一**
> （提示詞「a plain smooth plastered wall」）。本報告首版把它一起標成 fallback，
> **那是錯的**——臥室四面牆的 `surfaces_sources` 是 `clip`，不是 `fallback`。
> 會標錯的原因見 **§2.6 缺陷 F**：`data/materials.json` 與兩處註解都寫
> fallback 是 `generic_wall`，只有 `config.py:95` 的實際值是 `gypsum_board`。
> **單一事實來源缺失，讓討論建立在錯的前提上——這一段就是受害者。**

> **這條相關性在本資料集上證據不足，必須誠實說明**：拿 §7-2 八個場地的
> 「fallback／域外面數」對「聯合帶誤差」算相關係數，**含沙灘 r = −0.57、
> 去掉沙灘 r = +0.90**——n=8 拿掉一個點就整個翻向。
> **這種相關係數不能當證據**，本報告不據此宣稱 fallback 是系統性高估的主因。
> 站得住的只有上面那張 α 表（直接讀數字，不需統計）。

### 1.4 盲性如何保證（可複驗）

`scripts/t17_blind_test.py`：① 檔名只有 `sample_N`，不含空間名與來源照片名；
② 順序由**固定種子 20260830** 打亂（照原順序改名等於沒打亂）；
③ 五個檔 mtime 全部對齊，避免用檔案時間反推。
試聽頁 `播放頁.html` 與其 HTML 原始碼同樣不含答案（實測 5 個空間選項各出現 10 次、
完全對稱，view source 推不出對應）。五個檔皆非靜音（RMS 0.0283–0.0357）。

> ⚠️ **限制**：乾聲只有 `assets/dry/clap_synth.wav`（合成拍手），真實說話乾聲仍待補。
> 用拍手做空間配對比人聲**難**，所以 2/5 這個分數偏**保守**。
> 但本項未達標的主因不是乾聲——是 §1.2 的尺寸反轉，換乾聲救不回來。

## 2. §7-2 RT60 對照 —— ❌ 未達標

### 2.1 量測方法（裁決 B 執行要求）

| 要求 | 落實方式 |
|---|---|
| 生成 IR 與真實 IR 用**同一支未經修改**的量測管線 | 兩邊都走 `src/image_reverb/ir_metrics.py` 的 `band_t30()` 與 `t30_low_combined()`。`scripts/t17_rt60_table.py` 不重新實作任何量測邏輯 |
| `ir_metrics.py` 既有程式 T-17 期間 git diff 為空 | ✅ `git diff -- src/image_reverb/ir_metrics.py` **輸出為空**（本次驗收未動 `src/` 任何一行） |
| 聯合帶參數 88.4–353.6Hz 固定，不得逐場地調整 | ✅ 寫死在 `ir_metrics._LOW_COMBINED_*`，本報告全部場地共用同一組 |

**立體聲真實 IR 的處理**：EchoThief 五場地是 stereo。**不做聲道相加**——兩聲道相加會在高頻
產生梳狀濾波，污染 2k/4k 的量測。改為逐聲道各量一次 T30 取平均，逐聲道值保留在
`rt60_table.json` 的 `per_channel` 欄可供審查。

**截尾檢查**：每一筆都記錄 Schroeder 曲線最低點。13 條真實 IR 的曲線最低點落在
**−90dB 至 −inf**，−5→−35dB 的擬合區完全在資料內，**無截尾偏差**。

### 2.2 完整誤差表與分組達標率

完整表格（8 場地 ×（6 頻段＋聯合帶）、分組達標率、階梯比、手動尺寸依據）由
`scripts/t17_report_tables.py` 產生，見 **[tables.md](tables.md)**。以下摘錄關鍵結果。

**分組達標率（裁決 C：不得合併成單一數字）**

| 組別 | 五項硬判準通過 | 全達標場地 |
|---|---|---|
| **自動幾何**（`metric_depth` / `equirect_multiview`，F-01 產品主張本體） | **9/40（22%）** | **0/8** |
| **手動尺寸**（`manual`，F-09 正式出口） | **5/25（20%）** | **0/5** |

> **對自動路徑的單獨結論（裁決 C 要求）**：F-01 所主張的「照片 → 自動幾何 → IR」這條路徑，
> 在 8 個有真實 IR 對照的場地上**沒有任何一個場地通過 §7-2**，五項硬判準的整體通過率
> 只有 22%。**自動路徑目前不具備 SPEC §7-2 意義下的準確度。**
>
> 手動尺寸組（20%）**並沒有比自動組好**——這是本次驗收最重要的訊號之一：
> 把幾何換成（近似）正確的值幾乎沒有改善結果，代表**誤差的主導來源不是幾何**。

**逐 run 摘要**

| run | dims_source | conf | 500Hz–4kHz 通過 | 聯合帶 | 125Hz | 250Hz |
|---|---|---|---|---|---|---|
| `CathedralRoom` | equirect_multiview | medium | 0/4 | ❌ +186% | +45% | +191% |
| `DivorceBeach` | equirect_multiview | low | 0/4 | ❌ +676% | +373% | +651% |
| `site_photo_department_store` | metric_depth | medium | 1/4 | ❌ +121% | +77% | +113% |
| `t17_manual_department_store` | manual | high | 1/4 | ❌ +162% | +126% | +154% |
| `site_photo_gym` | metric_depth | low | 0/4 | ✅ −20% | −18% | −17% |
| `t17_manual_gym` | manual | high | 0/4 | ✅ −17% | −14% | −13% |
| `site_photo_restaurant` | metric_depth | low | 1/4 | ✅ −4% | −17% | −3% |
| `t17_manual_restaurant` | manual | high | 0/4 | ❌ +80% | +54% | +98% |
| `RacquetballCourt4` | equirect_multiview | medium | 0/4 | ❌ −50% | −68% | −46% |
| `t17_manual_racquetball` | manual | high | 0/4 | ❌ −61% | −76% | −52% |
| **`SteinmanHall`** | equirect_multiview | low | **4/4** | ❌ +30% | +17% | +47% |
| `t17_manual_steinman` | manual | high | 3/4 | ❌ +38% | +24% | +55% |
| `TunnelToHell` | equirect_multiview | medium | 0/4 | ✅ +0.2% | −40% | +2% |

**唯一接近達標的是 `SteinmanHall`**：500Hz–4kHz **四個頻段全部通過**（+16%／+6%／−3%／+9%），
只差低頻聯合帶 +30% 一項。這是 8 個場地中唯一一個「產品主張的自動路徑幾乎做對了」的案例。

### 2.3 裁決 B 的事後檢驗 —— 誠實回報：本資料集上聯合帶沒有改變低頻達標率

裁決 B 把低頻判準從逐頻段 125/250Hz 換成聯合帶，理由是逐頻段低頻是**不可信的量測**
（T-18 已由 Opus 獨立重現該混頻機制：異速鄰帶會讓 125Hz 量到 +105%）。
機制本身確實成立。但在本次 8 場地的實測上：

| 判準 | 13 個 run 中通過數 |
|---|---|
| 低頻聯合帶 | **4/13** |
| 逐頻段 125Hz | 4/13 |
| 逐頻段 250Hz | 4/13 |

**三者完全一樣。** 解讀：本資料集上，低頻誤差的主導來源是**模型預測本身就錯**
（誤差量級 +45% ～ +676%），遠大於裁決 B 要消除的量測混頻偏差（量級 ~+100%）。
換句話說——**裁決 B 是對的但不夠用：它修好了量測，可是被量測的東西本來就是錯的。**
這不是推翻裁決 B（量測正確性本身有獨立價值），而是說它不是 §7-2 未達標的原因。

**殘留風險檢查（裁決 B 自陳，逐場地列出）**：聯合帶上緣 354Hz 與 500Hz 帶共享邊緣，
若某場地 500Hz T30 比聯合帶慢 2 倍以上，聯合帶量測可能被拉長。
實測 8 場地的 `T30(500Hz)/T30(聯合帶)` 階梯比落在 **0.669 – 1.259**，
**沒有任何一個場地接近 ≥2 或 ≤0.5 的觸發區**（完整逐場地表見 tables.md 表 3）。
→ **裁決 B 自陳的殘留風險在本資料集上沒有發生。**

### 2.4 🔬 病因隔離：問題在材質辨識，不在幾何、不在 IR 合成引擎

必測反例 `racquetball_court_4`（最小空間、最長殘響）**被做成短殘響 → 依卡片規定直接記為未達標項**。
但更重要的是它讓病因被隔離出來。三個 run 用**同一支合成引擎**，只換輸入：

| run | 尺寸 | 材質 | 125Hz | 500Hz | 1kHz | 聯合帶 |
|---|---|---|---|---|---|---|
| `RacquetballCourt4`（自動） | 16.10×9.39×5.55（估錯） | CLIP：west=**curtain_fabric**、south=glass、floor=wood_panel | −68% | −50% | −41% | **−50%** |
| `t17_manual_racquetball`（F-09） | **12.19×6.10×6.10（官方標準尺寸）** | 同上（CLIP 未變） | −76% | −60% | −52% | **−61%** |
| `t17_diag_racquetball_hard`（診斷） | 同上官方標準尺寸 | **人工改對**：floor=wood_panel、其餘五面 concrete | **−3%** | +28% | +74% | **+13%** |

**推論鏈**：

1. 把幾何從「估錯的 16.1×9.4×5.6」換成「官方標準 12.19×6.10×6.10」，結果**不但沒好，還更差**
   （−50% → −61%）。→ **幾何不是主因。**
2. 幾何不動、只把材質改對，同一支引擎立刻從 −50% 變成 **+13%**（低頻聯合帶），
   125Hz 誤差 −3.3%。→ **材質就是主因。**
3. 具體的錯誤是 CLIP 把壁球場的一面牆判成 `curtain_fabric`（厚窗簾布）。壁球場裡不存在窗簾；
   而 `curtain_fabric` 是資料表中吸音最強的材質之一，一面就足以把 3 秒殘響吃成 1.4 秒。

這**直接印證 HANDOFF 地雷 #13**（「CLIP zero-shot 的 top-1 機率不能單獨當信心指標」）：
該筆判定並未觸發任何警示——`surfaces_sources` 顯示它是正常的 `clip` 結果，不是 fallback、
不是 out_of_domain。**模型高高興興地給了一個會毀掉整條 IR 的答案。**

> 診斷 run 剩下的 +28%～+74%（500Hz–2kHz 偏長）是另一個獨立議題：混凝土 α 極低、
> Sabine 在小而硬的空間高估、且引擎未建模空氣吸收。這一項**沒有被本次驗收隔離**，
> 留給 Fable 決定是否進修正輪。

### 2.5 🐛 新發現的缺陷（本次驗收發現，先前未記錄）

#### 缺陷 A（🔴 高）：`is_equirect()` 只看長寬比，把 2:1 的一般透視照誤判成 360° 環景

`TunnelToHell.jpg` 是 **2592×1296 = 正好 2.000** 的一般透視照片
（`SOURCES.md` 與 `INFO.md` 都明載「一般透視（iPhone 4）」、「✅ 可直接餵給深度／分割模型」）。
決定性佐證：**EXIF 記錄原始影像高度為 1936**（iPhone 4 的 4:3 感光元件），
`Software=Adobe Photoshop CS5` —— 這是一張被裁成 2:1 的透視照，不是球面全景。
目視也確認是單點透視消失點的隧道照。

`preprocess.is_equirect()` 的實作是：

```python
def is_equirect(img, aspect_ratio=2.0, tolerance=0.05) -> bool:
    """只看長寬比是否 ≈ 2:1（容差 ±5%），不看檔名。"""
    w, h = img.size
    return abs(w / h - aspect_ratio) <= aspect_ratio * tolerance
```

→ 這張照片被**靜默地**送進環景路徑，做了 6 次球面重投影。輸出：

| | 尺寸 | dims_source | confidence |
|---|---|---|---|
| 誤判走環景路徑（**產品實際行為**） | 10.48×2.48×6.67 | equirect_multiview | **medium** |
| 裁成 2400×1296 破壞 2:1 後走透視路徑（診斷） | 15.61×18.03×9.82 | metric_depth | low |

**兩個都錯**（真實是隧道，寬度約 3m 而非 2.48m 或 18.03m），但誤判那條路徑拿到了
**沒有依據的 `medium`**，而正確路徑至少誠實標了 `low`。

這是本專案「安靜地輸出看似合理的錯誤結果」的**第六次**
（前五次：地雷 #2 洞二、#9、#12、#13、#15）。
HANDOFF 地雷 #11 記錄的殘留限制是**反方向**的（帶外框的 equirect 被當成一般照片），
**這個正向誤判先前沒有被記錄過。**

#### 缺陷 B（🟠 中）：`--override-dims` 一律給 `confidence: high`，但材質仍是猜的

五個 `manual` run 全部回報 `confidence: high`，然而它們的**材質完全沒被驗證**——
且肉眼可見是錯的：

| run | CLIP 判定的地板 | 實際 |
|---|---|---|
| `t17_manual_department_store` | `acoustic_panel` | 賣場磨石／水泥地 |
| `t17_manual_gym` | `acoustic_panel` | 橡膠地墊 |
| `t17_manual_racquetball` | west 牆 = `curtain_fabric` | 硬質牆（§2.4 已證這一項毀掉整條 IR） |

`confidence` 只反映幾何來源，**卻是使用者唯一看得到的信任訊號**。
手動輸入尺寸後拿到 `high`，會讓使用者以為整份分析都可信。
建議（不在本卡範圍，交 Fable）：`confidence` 拆成 `geometry_confidence` 與
`materials_confidence`，或在 manual 路徑下以材質信心為準取 min()。

#### 缺陷 C（🟡 低）：戶外場地無結構性出口

`divorce_beach` 是戶外沙灘（SPEC 未排除此類輸入）。ShoeBox 房間模型在物理上無法表示開放空間，
結果是全表最大誤差（聯合帶 **+676%**、1kHz **+959%**）。管線有標 `confidence: low`，
但**沒有任何一條警示說「這看起來是戶外，本模型不適用」**——分割結果其實有 sky 類別可用
（T-06 防呆規則已經在偵測 sky，只是用途是打折材質信心，不是拒絕輸出）。


### 2.6 🐛 追加缺陷（2026-08-30 外部診斷提出，Opus 逐條查證後確認）

以下四項由一份外部 bug 診斷提出，**本報告不採信轉述，逐條讀碼＋執行期重現後確認全部屬實**。

#### 缺陷 D（🔴 P0）：ADE20K「語意可信材質」分支是 dead behavior

`src/image_reverb/surfaces.py:37-48` 宣告 `ADE_TRUSTED_MATERIAL`，註解寫
「語意可信、可直接映射材質的類別——**這些不必問 CLIP**」；`:238` 的迴圈註解再寫一次
「有就直接映射，不必問 CLIP」。實際上：

- `:242` 算出 `best_trusted` 後，`:244` **無條件呼叫** `classify_region_material()`；
- `best_trusted` **只被用在 `:262-265` 串一段 `note` 字串**；
- `:268-270` 的 `material_id=mid` 永遠是 CLIP 的結果。
- 全專案 grep：`"ade_trusted"` **只出現在 `:115` 記錄 method 可能值的註解裡，從未被指派**。

**Opus 執行期重現**（樁掉 segmenter 與 CLIP，CLIP 固定回 `concrete` 且信心 0.95）：

```
配置：windowpane 40%（語意可信 → glass）、wall 30%、floor 20%、ceiling 10%
floor    material_id=concrete   method=clip
ceiling  material_id=concrete   method=clip
wall     material_id=concrete   method=clip
```

可信類別佔 40%，三個角色的材質**全部仍是 CLIP 的 concrete**。分支確認無作用。

**附帶發現（比原診斷更嚴重）**：`:239` 的 `trusted_hits` 用的是
`segment_roles()` 回傳的**全圖** `ratios`（`:154` 是 `count/total_pixels`），
**沒有被角色 mask 限制**。上面重現中 windowpane 全部在畫面上半，
`floor` 與 `ceiling` 的 note 卻都宣稱「40% 像素屬語意可信類別 → glass」。
→ 就算把 `if` 補上去，**現在的計分方式也是錯的**，不能直接啟用。

> 這條與 §2.4 的壁球場病因相互獨立又互相加強：材質路徑上**同時**有
> 「CLIP 在候選集內判錯」與「本來要救 CLIP 的可信分支從未生效」兩個問題。

#### 缺陷 E（🔴 P0）：已判定不可信仍無條件輸出 WAV

`src/image_reverb/pipeline.py:225-239` 從幾何直接進聲學 → 合成 → `export_ir()` →
輸出 wet preview，**中間沒有任何一行檢查 `est.confidence` 或域外狀態**。
本報告 §1.2 已從產品面記錄過這件事（體育館與車內的防呆都正確作動卻照樣出貨），
外部診斷補上了精確的程式位置。**兩者是同一個問題的兩個視角。**

#### 缺陷 F（🟠 P1）：fallback 材質的定義四處不一致

| 位置 | 宣稱／實際 |
|---|---|
| `data/materials.json:10` | `"fallback_id": "generic_wall"` |
| `src/image_reverb/config.py:95` | `DEFAULT_WALL_MATERIAL = "gypsum_board"` ← **實際執行值** |
| `config.py:103` 註解 | 「低於此值就 fallback **generic_wall**」 |
| `surfaces.py:167` docstring | 「fallback `generic_wall`」 |

`classify_region_material()` 的三個 fallback 出口（`:174`／`:198`／`:201`）
全部回傳 `config.DEFAULT_WALL_MATERIAL`，即 **`gypsum_board`**。
→ 資料檔、兩處註解、以及本報告 §1.3 的討論都指向 `generic_wall`，但跑起來是 `gypsum_board`。
兩者 1kHz α 分別是 0.03 與 0.04，聲學後果接近，**但這是單一事實來源缺失，
會讓任何關於 fallback 的討論建立在錯的前提上**（§1.3 就受此影響）。

#### 缺陷 G（🟡 P2）：`scripts/t17_blind_test.py` 可接受舊產物 —— ✅ 已修

原本只檢查 `wet_preview.wav`／`ir_mono.wav` 存在就複製，等於允許「拿舊產物驗收新程式」。
**這是本卡自己的交付物，已當場修正**：現在核對 `analysis.json` 記錄的來源照片、
比對產物與照片 mtime 先後，並輸出 `blind_test/MANIFEST.json`
（git revision＋`src/`/`data/` 是否 dirty＋三個檔的 sha256）。

**護欄實測會觸發**（不是加了就算數）：

```
情境 A（照片比產物新）→ exit 1「analysis.json 比來源照片舊，產物過期」
情境 B（記錄來源對不上）→ exit 1「analysis.json 記錄來源是 WRONG_PHOTO.png」
情境 C（正常）→ exit 0，五個盲聽檔 MD5 未變
```

> **本輪 2/5 的結果不受影響**：五個盲聽檔的 MD5 在修正前後逐位元相同，
> 且首版已用 MD5 核對過與來源 `wet_preview.wav` 一致。

---

## 3. §7-3 外部相容性 —— ✅ 通過

**使用者實測回報（2026-08-30）**：載入 convolution reverb「**可以的，有殘響**」。
→ SPEC §7-3「匯出的 WAV 可直接載入 Altiverb / Space Designer / 任一 convolution reverb
正常使用」**達標**。

Opus 事前驗證的格式（`soundfile.info` ＋ `file`）：

```
output/bathroom_tiled/ir_mono.wav    48000Hz 1ch PCM_24 WAV
output/bathroom_tiled/ir_stereo.wav  48000Hz 2ch PCM_24 WAV
file → RIFF (little-endian) data, WAVE audio, Microsoft PCM, 24 bit, mono 48000 Hz
```

測試用檔案：`output/mvp_acceptance/blind_test/sample_3_IR.wav`（原始 24-bit，5.60s）。

> 這是四項標準中**唯一乾淨通過**的一項。值得注意的是它驗的是**格式與管道**，
> 不是內容正確性——WAV 能載入、有殘響，與那條殘響是否對應照片裡的空間是兩回事
> （後者由 §7-1／§7-2 回答，兩項都未達標）。

## 4. §7-4 人耳試聽 —— ✅ 已執行並記錄

**使用者回報原文（2026-08-30）**：

> 有差異。沒有鐵筒子的聲音，但是材質改對的那一個版本 Reverb Time 大概可以在減少 1 至 1.5 秒

**三項結論：**

### 4.1 ✅ 沒有「鐵筒子」artifact —— 地雷 #9 的修正確認仍然有效

HANDOFF 地雷 #9 記錄的「像用手拍鐵筒子」（殘響能量全集中在 30–135Hz）
**在本次九個試聽檔中都沒有出現**。T-12 的逐表面材質介面修正到今天仍然站得住。

### 4.2 ✅ 使用者的耳朵獨立確認了 §2.4 的病因診斷

使用者聽出壁球場材質判錯版 vs 材質改對版「**有差異**」。
兩者是**同一支合成引擎、同一組官方標準尺寸**，唯一差別是材質判定
——這用耳朵獨立佐證了 §2.4「病因在材質、不在幾何也不在引擎」的結論。

### 4.3 🔬 「還應該再少 1–1.5 秒」——聽感與量測數字互相印證

使用者認為材質改對的版本殘響仍然過長，應再減 1–1.5 秒。對照量測：

| 頻段 | 診斷 run 量測 | 真實 IR | 差距 |
|---|---|---|---|
| 1kHz | **5.085s** | 2.926s | **+2.16s** |
| 2kHz | 4.429s | 3.009s | +1.42s |
| 4kHz | 3.461s | 2.755s | +0.71s |
| 低頻聯合帶 | 3.110s | 2.755s | +0.36s |

**使用者估的「1–1.5 秒」正好落在 2k/4k 的實測超出量（+1.42s／+0.71s）之間**，
而中頻 1kHz 超出更多（+2.16s）。**耳朵與量測指向同一個方向、同一個量級。**

這條回饋很有價值：它證實 §2.4 末尾那段「診斷 run 剩下的 +28%～+74% 偏長是另一個
獨立議題」**不是量測假象，是真的聽得出來的**。可能原因（未在本次隔離）：
`concrete` 的 α 極低、Sabine 在小而硬的空間高估、引擎未建模空氣吸收。

> **方向性提醒給 Fable**：§7-4 的聽感（殘響過長）與 §7-2 表格的多數場地一致
> （洞窟 +186%、賣場 +121%、臥室 3.56s）。**「系統性偏長」是跨越量測與聽感的
> 一致訊號**，比任何單一場地的數字都值得優先處理。

## 5. 素材與方法的限制（誠實聲明）

這些限制**削弱**本報告的證據力，必須與結論一起讀：

1. **MIT 三場地不是嚴格的 photo↔IR 配對。** 各 `INFO.md` 原文明載
   「**不可**當成嚴格的 photo→IR ground-truth pair 來評分模型」——MIT 從未公開對應表。
   gym 有 3 條 IR（聯合帶 0.438 / 1.163 / 1.469 s，**相差 3.4 倍**）、restaurant 有 4 條
   （0.361 – 0.603 s）。本報告的處理是**不挑一條當真值**，取中位數為參考值並在表中標
   `🟡` 表示「對中位數超差但落在該場地多條 IR 的區間內」的弱命中。
   → **這 3 個場地的達標／未達標判定證據力弱於另外 5 個。**
2. **手動尺寸只有一個有權威來源。** `t17_manual_racquetball` 用的是國際壁球場公開標準
   （40×20×20 ft）；其餘四個是 **Opus 依照片估的**，逐項依據列在 tables.md 表 4。
   尤其 `t17_manual_restaurant` —— **那張照片只拍到一個卡座，室內尺寸根本不可見**，
   該筆數字幾乎是純猜測。→ **手動組 20% 的達標率不應被當成「給定正確幾何後的真實表現」**，
   它只是「給定 Opus 估的幾何後的表現」。
3. **EchoThief 的 IR 經過峰值正規化且可能經過修剪**，尾段若被處理過會讓真實側 T30 偏短。
   本報告未能獨立驗證這件事。
4. **§7-1 的樣本數只有 5，且只有一位聽者、聽一次。** 2/5 與「隨機期望 1.0」的差距
   在統計上不顯著——**這個分數不能單獨拿來下「產品聽起來如何」的結論**。
   它的價值在 §1.2 的**錯誤結構**（大小系統性反轉，且每一題都與量測到的尺寸一致），
   那個結構不是靠 n=5 支撐的，是靠逐案量測支撐的。
5. **§7-4 是主觀聽感，只有一位聽者。** 「再少 1–1.5 秒」是估計值不是量測值；
   本報告用它做的唯一推論是「方向與量級和量測一致」，沒有拿它當判準。
6. **📷 照片來源網址仍未補齊**（`assets/SOURCES.md` §2，9 張全部）。依 **Fable 裁決 E**，
   這是結案前置：**本卡狀態最高停在 🔵**，不得改 ✅。

---

## 6. 給 Fable 的決策輸入

**已經有數據支撐的判斷：**

0. **§7-1 與 §7-2 是兩條獨立的證據鏈，但它們指到的不是同一個病因——不要合併看。**
   - **§7-2（量測）的主導病因是材質**：壁球場 −50% → 材質改對 +13%，這條成立。
   - **§7-1（耳朵）的三個錯題根因各不相同**：sample_1 幾何量程、sample_2 域外輸入、
     sample_4 材質。只有 1/3 與 §7-2 的結論重疊。
   → **正確的讀法是「材質是 §7-2 的主導病因，也是 §7-1 三個錯題之一的病因」**，
   不是「兩條路徑都證明問題在材質」。修正輪若只打材質，sample_1／sample_2 這一類
   （幾何量程、域外輸入）**不會被修好**。
1. **修正輪應該打材質，不是打幾何。** §2.4 是決定性的：同一支引擎，只換材質就從 −50%
   翻到 +13%。而把幾何換成官方標準尺寸**反而更差**。目前 `T-11 換 Metric-Indoor-Large`
   的提案（ROADMAP 延後至 T-17 後評估）——**本次數據不支持優先做它**：
   手動組（近似正確幾何）達標率 20%，並沒有比自動組 22% 好。
2. **CLIP zero-shot 的域外處理還不夠。** 地雷 #13 加的 `__vehicle_interior` 之類選項救了車內，
   但壁球場那面牆是**在候選集內被判錯**（判成 curtain_fabric），域外選項救不到這種錯。
   需要的是另一個層次的對策（材質專用模型？空間類型先驗約束候選集？）。
3. **`is_equirect()` 必須修**（缺陷 A）。這是可以立刻修掉的靜默失敗：長寬比之外再加一個
   極點列均勻度檢查（equirect 的第一／最後一列依定義完全均勻，透視照不會）——
   HANDOFF 地雷 #11 已經記錄了這個性質，只是當時用在反方向。
4. **`confidence` 語義需要拆**（缺陷 B）：目前它只講幾何，卻是使用者唯一的信任訊號。

5. **🔴「偵測到不可信之後照樣出貨」是一個獨立於準確度的產品問題。**
   §1.2 顯示體育館與車內兩筆，管線的防呆規則**都正確作動了**（`low` ＋ 明確警示原文），
   但產品仍然輸出了一條聽起來完全是別的空間的 IR，使用者盲聽時當然照樣配錯。
   → **降信心不等於保護使用者。** 需要裁決 `low` 是否該是「拒絕輸出／強制要求手動尺寸」，
   而不只是 JSON 裡的一個欄位。
6. **系統性偏長是跨量測與聽感的一致訊號**（§4.3）：§7-2 多數場地高估、§7-4 聽感也說過長。
   比單一場地的數字更值得優先處理。
7. **fallback 材質的預設值選擇值得單獨查證**（§1.3）：實際 fallback 是 `gypsum_board`
   （1kHz α=0.04，12 種裡第 4 不吸音）。機制清楚，但**本資料集的相關性證據不足**
   （去掉一個點 r 就從 −0.57 翻成 +0.90），不要靠這份資料下結論。
8. **🔴 材質路徑上有兩個獨立缺陷，不是一個**（§2.6 缺陷 D）：除了 §2.4 的
   「CLIP 在候選集內判錯」，**本來要救 CLIP 的 ADE 語意可信分支從未生效**
   （`method` 永遠不會是 `ade_trusted`），且其計分用全圖比例、未被角色 mask 限制
   ——**補一個 `if` 就啟用會直接引入新錯誤**。修正輪要把這兩件事分開處理。
9. **ShoeBox 六面模型表達不了室內陳設**（§1.2 sample_4）：臥室的牆 CLIP 判得沒錯
   （`source: clip`，提示詞「a plain smooth plastered wall」），
   錯在床／棉被／窗簾／地毯**無處可放**。這是**模型結構限制，不是辨識準確度問題**——
   需要 occupancy／等效吸音面積之類的機制，換更好的分類器救不到。
10. **`fallback` 缺單一事實來源**（§2.6 缺陷 F）：資料檔說 `generic_wall`、
   實際跑 `gypsum_board`、兩處註解各說一次。**本報告 §1.3 首版就是被這個誤導而標錯**，
   建議修正時加 invariant test。

**需要 Fable 裁決的：**

- **修正輪的範圍必須明確**：只打材質**不足以**修好 §7-1 的 sample_1（幾何量程）
  與 sample_2（域外輸入）。要一次打三條（幾何 gate／域外 gate／材質），還是分卡？
- **缺陷 D 怎麼處理**：修好並啟用 ADE 可信分支，還是先移除死碼與誤導性註解、
  等 occupancy 機制設計好再一起做？（直接補 `if` 會引入新錯誤）

- §7-2＋§7-1 皆未達標 → 加修正輪？還是接受現況、把驗收目標往後移到材質模組升級之後？
- **`confidence: low` 要不要升級成「拒絕輸出」或「強制手動尺寸」**（上面第 5 點）？
  這是本次驗收新浮出來的產品層問題，不在原本的 ROADMAP 裡。
- 是否要為戶外／非矩形空間加「拒絕輸出」的出口（缺陷 C），還是列為 SPEC 適用範圍外？
- 診斷 run 剩下的 500Hz–2kHz +28%～+74% 偏長（§2.4 末、§4.3 已由聽感佐證），
  要不要單獨開卡追？
- fallback 材質預設值（第 7 點）要不要開卡？
- §7-1 是否要在補到真實人聲乾聲、或修好材質之後**重測一次**？
  （本次 2/5 的主因是尺寸反轉，換乾聲救不回來，但修好材質後應該重驗）

---

## 7. 可重跑複驗

本報告的每一個數字都由下列指令產生，無一手打：

```bash
source .venv/bin/activate
python scripts/t17_rt60_table.py      # 量測 → output/mvp_acceptance/rt60_table.json
python scripts/t17_report_tables.py   # 統計 → output/mvp_acceptance/tables.md
python scripts/t17_blind_test.py      # §7-1 盲聽素材（固定種子，可重現同一組打亂）
```

生成側 IR 的重跑指令逐一列在 `rt60_table.json` 的 `run` 欄；`manual` 組的
`--override-dims` 參數列在 tables.md 表 4。
