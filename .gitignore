# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Env
.env
.env.local

# Node
node_modules/
dist/
build/

# Python
__pycache__/
*.pyc
.venv/
venv/

# 產出的音檔/圖檔不進 git，但 REPORT 等 .md 分析報告要進版控
# （REPORT.md 是 T-05/T-06 最有價值的產出，不能被 rm -rf output/ 清掉）
output/**
!output/**/
!output/**/*.md

# 第三方對照 IR 與場地照片：可免費下載供本機開發，但未取得再散布授權
# （EchoThief © Dr. Chris Warren；MIT Reverb Survey 見各 INFO.md）
# 只讓 INFO.md 進 git 保留出處紀錄，媒體檔請依 INFO.md 的網址自行下載
assets/reference_irs/**/*.wav
assets/reference_irs/**/*.jpg
assets/reference_irs/**/*.jpeg
assets/reference_irs/**/*.png

# Logs
*.log

# T-04 replaced legacy photos: local reproducibility backup, not active inputs
assets/photos_legacy_20260920/

# 使用者自拍的真實空間素材（T-64；Fable 2026-09-21）：照片與錄音預設不進 git（隱私；repo 會 push 到遠端），
# 只有 README／資料表／manifest／GT 進版控。使用者明確同意才由 T-64 拿掉對應規則。
assets/incoming_real/**
!assets/incoming_real/
!assets/incoming_real/README.md
!assets/incoming_real/空間資料表.md
assets/photos_real_dev/**
assets/photos_real_heldout/**
!assets/photos_real_dev/**/
!assets/photos_real_heldout/**/
!assets/photos_real_dev/**/*.json
!assets/photos_real_heldout/**/*.json
!assets/photos_real_dev/**/*.md
!assets/photos_real_heldout/**/*.md
