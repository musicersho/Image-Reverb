# Image Reverb 完工差距檢視（2026-09-20）

檢視版本：`f3e07b0`。範圍：SPEC、ROADMAP、TASKS、TODO、HANDOFF、現行程式入口與設定、既有驗收／校準／T-58 報告、素材路徑；不是逐行安全稽核或全模型重驗。

## 現況

核心 Python CLI 已有照片、文字與複合場景三條路徑、逐頻段聲學、IR 合成、WAV 匯出、離線卷積及分析圖。T-15／T-16／T-18 任務卡已通過，ROADMAP 與 TODO 部分未勾選屬過期摘要，不能算成尚未實作。照片／文字已有簡單去相關 stereo；複合場景仍 mono，不宜把「所有 stereo 都沒做」列為待辦。

MVP 仍是首驗 FAIL：盲聽 2/5（需 ≥4/5），真實 IR 對照全判準達標場地自動組 0/8、手動組 0/5。這是 2026-08-30 舊版結果，不是本次新圖重跑結果。後續 gate_calibration_v3 的舊 13 張基準圖 default 與 role_aware 都是 13/13 BLOCK；防止錯誤輸出已改善，實際自動可用性仍未證明。

目前 repo 未見 JUCE／CMake 外掛專案或可安裝桌面 UI；現有 analysis.png 是分析輸出，不是可拖圖、即時調整與試聽的應用介面。

## 距離 CLI MVP 的必要順序

1. **整理資料集與路徑**：上一輪 T-04 換圖後，`assets/photos/` 改用 `t04_gpt_*`；`t17r2_common.LEGACY_PHOTOS_DIR` 與 `t17_blind_test.SPACES` 仍期待舊五張。本次唯讀查找五張全部 None，備份區五張全存在。`--legacy` 又禁止搭配 `--photos-dir`，因此既有降級方案不能直接執行。這是本次照片替換留下的整合缺口：需在 R2 凍結前規劃修正 legacy 資料路徑，或明確復原歷史測試集；不能把新圖改成舊名冒充舊 GT。歷史重現使用原版 checkout。新開發圖、舊基準圖、正式 held-out 必須分開管理。
2. **T-57-F1 正式審查**：目前工程待審，正式驗收通過的 commit 才满足 R2 前置。本次重跑 `scripts/test_t17r2_tools.py` 顯示「全部通過」，不取代任務卡完整 Opus 審查。
3. **正式驗收素材就位**：`assets/photos_heldout/` 與 `ground_truth_heldout.json` 尚不存在；目前 GPT Image 候選皆長邊 1448px，未達原卡 1920px。還需處理真實素材／合成證據定位、未用於調參的資格、尺寸與逐面材質確認、來源表。生成提示詞不是實測 GT。若改走原卡降級方案，需使用者明確選擇且先解決步驟 1。現存五張候選 PNG 未追蹤，R2 要求工作樹乾淨，需先正式管理，不能直接刪掉。
4. **執行 T-17-R2**：鎖定資料集與版本 → 預設／forced／手動分組產樣本 → 新盲聽 → 真實 IR 的 RT60 對照 → 外部卷積效果器載入 → 使用者聽感 → 報告 coverage、誤放與四軸判定。不可在產樣本與打包間改碼／commit；首驗 FAIL 永久保留。
5. **依結果改善再驗收**：R2 卡事前判定只有 mit_gym 屬 in-domain，既有結果被 gate 擋下，自動 coverage 很可能 0/1；這是預期風險，不是已執行的 R2 結果。需按 R2 實證針對材質、幾何／域外行為及聲學誤差開修正卡，再用獨立資料重驗。不得藉放寬信心門檻或換漂亮照片宣稱改善。

T-04 已有使用者「內部使用、維持未結案」決定，不必再次要求補舊網址；仍需在 R2 報告如實列缺項。T-42／43／46／48／52／54／55 已有結案紀錄；T-56／T-58 也已工程結案，不要重開已完成任務。

## 品質改善分支（不是全部都必須先做）

- 材質辨識是既有真實 IR 對照的主要問題。若繼續 role_aware 路線：T-53 需 ≥13 張獨立校準圖、五類各 ≥2、使用者確認逐面 GT；再做獨立 T-44-R1，通過採用條件才啟用。這條目前不是 R2 硬前置，也不能承諾校準必能改善辨識。
- 巨蛋與車內不在一般 ≤10m 室內自動幾何範圍。巨蛋圖更新只改善素材，不擴大模型能力；大空間需可靠手動尺寸／材質出口，車艙也不能當一般矩形房間宣稱物理精準。
- T-59 在 R2 後規劃：診斷分頻段尾音互相影響造成的 RT60 誤差。T-58 顯示極端地毯條件與真實百貨樣本的 500Hz 有超差；不能把所有誤差一概歸材質，也沒有證據支持直接把 Sabine 換 Eyring／pra 就會更好。
- T-29 三路分析 JSON 一致性、T-50 archive 清理、README／ROADMAP／TODO 狀態同步，是後續整合與維護工作；目前不應擠到 R2 關鍵路徑前。

## 如果「完工」指 DAW 中能直接使用的完整產品

MVP 品質收斂後，Phase 2 尚需：JUCE AU／VST3 架構、即時 partitioned convolution、Wet/Dry／Gain／Pre-delay／Size／Damping／EQ、Python 推論整合方式、照片拖入與人工修正 UI、即時試聽、IR 庫、立體聲行為完善、DAW 相容性與 CPU／延遲／自動化測試、安裝與打包。發佈方式再決定簽章及公證需求。

影片、多幀融合、可移動聲源、神經 IR、Windows 是 Phase 3／後續；不應混入「先交付第一版」的必做清單。

## 判斷與建議

目前是「核心引擎已成形、照片自動化品質未達標、外掛尚未開發」，不能用任務勾選率推算完成百分比。CLI 手動輔助工具距離交付較近；可靠全自動照片重建仍含研究不確定性；完整外掛還有一整階段產品工程。

優先順序：資料集路徑整理＋T-57-F1 審查 → 正式素材／程序確定 → R2 → 只針對失敗證據開改善卡 → 通過後做外掛。若希望先交付「照片輔助＋人工確認／文字 preset」版本，應另立產品範圍與驗收卡，不把它冒充原照片全自動 MVP PASS。

本次四軸：工程＝盤點完成（非任務工程驗收）；實驗＝未新增；產品＝未改動；MVP＝沿用首驗 FAIL、R2 待重驗。未跑照片推論／新 IR、未更動產品碼或驗收門檻。
