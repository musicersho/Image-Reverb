# T-38 執行交接 — CLIP 提示詞治療（Phase 1.9 治療輪 2/3）

> 這份是**下一階段的執行交接**。上一階段（T-37 → T-40 → T-41 三張基礎建設卡）
> 已全部 ✅ 通過並 push，接下來要開始真正的「治療」。
>
> - **使用者**：只要讀「§1 白話版」和「§6 你要做的事」，其他章節是給 Claude 讀的。
> - **Claude（任一模型）**：先讀 [CLAUDE.md](CLAUDE.md) 知道自己的角色 → 讀本檔
>   → 讀 [TASKS.md](TASKS.md) 的 **T-38 卡**逐字執行。本檔**不取代卡片**，
>   只補卡片沒寫、但一定會踩到的操作細節。
>
> 建立於 2026-08-31（Opus 驗證完 T-41 後）。T-39 結案後這份檔可以刪除。
> 現況若與本檔衝突，一律以 [DEV_LOG.md](DEV_LOG.md) 最新一筆與 TASKS.md 為準。

---

## §0 現在的狀態（一句話）

**基礎建設三張卡（T-37／T-40／T-41）全部 ✅ 通過，最新 commit `d590c5b T-41: 驗證通過`；
下一張是 T-38——第一張真的要「把 AI 認材質的能力調好」的卡。**

Phase 1.9 完整順序（不得跳號）：
**T-37 ✅ → T-40 ✅ → T-41 ✅ → 👉 T-38 → T-39（需使用者參與）→ T-42 → T-43 → 回 Fable 收尾複評**

---

## §1 白話版：T-38 到底在做什麼

**一句話：只改「問 AI 的問法」，不改任何規則、不改任何資料，看能不能讓它認材質認準一點。**

現在的狀況（T-36 量出來的）：AI 看照片猜六個面的材質，**猜對率大約一半**——
跟丟銅板差不多。其中**地板是最差的角色**（13 個地板只對 4 個）。這就是為什麼
13 張照片 100% 被 gate 擋下、為什麼 T-17 盲聽只有 2/5。

T-38 要做的，是把程式裡問 CLIP 模型的那句話（提示詞，例如「a photo of a
concrete wall」）換成更好的問法，然後**重新量一次 78 個面**，看正確率有沒有上升。

**T-38 不做的事**（這些是別張卡的範圍，做了就是超出範圍）：
- 不新增材質種類（那是 T-39，而且要你本人參與確認）
- 不動 gate 規則、不動門檻 0.4、不動信心計算邏輯
- 不動 ground truth（那是你已經逐面確認過的「標準答案」，一個字都不能改）

---

## §2 上一階段留給你的三件工具（為什麼要先做那三張卡）

| 卡 | 給了什麼 | 對 T-38 的意義 |
|---|---|---|
| **T-37 ✅** | `is_equirect()` 加極點列均勻度檢查 | `TunnelToHell.jpg` 不再被誤判成 360° 環景。它的六面現在才是真的透視裁切，**量測基線才乾淨** |
| **T-40 ✅** | 評測快取指紋 `scripts/eval_cache.py` | 你改完提示詞重量時，快取指紋對不上會**自動重跑**；不會發生「改了碼卻靜默量到舊結果」 |
| **T-41 ✅** | 透視照 SegFormer 去重 | 一張透視照的分割模型從載入兩次變一次，**重跑 13 張的時間砍掉約 25%**（實測單張 −3.8～−11.6 秒）。T-38 是反覆重跑的迭代輪，這筆省下來的時間會一直賺 |

---

## §3 三個一定會踩到的地雷（本檔存在的主要理由）

### 💣 地雷 A：預設指令從此永遠 hard fail（T-40 的刻意設計）

```bash
python scripts/t36_clip_accuracy.py
```

這行**不會再動了**，一定 exit 1，訊息會說「屬於凍結基線目錄」。這不是程式壞了，
是 T-40 刻意做的：`output/clip_accuracy/` 是 T-36 已結案的診斷基線，不許被覆寫。

**正確做法**：治療評測一律加 `--out-dir` 指到新目錄（卡片指定 `output/clip_treatment/`）。
子目錄也算凍結（`--out-dir output/clip_accuracy/xxx` 一樣會被擋），別想繞。

### 💣 地雷 B：harness 的「T-33 凍結快取交叉守門」會在你還沒改任何東西前就 🔴 卡關

`t36_clip_accuracy.py` 的 `cross_check_against_frozen_baseline()`（第 188 行）
會拿本次判定去比對 T-33 凍結快取，不一致就 `sys.exit(1)`。

**這已經是事實了，我（Opus）實測確認**：`TunnelToHell` 的 surfaces／sources
在 T-37 之後已經和 T-33 凍結快取不同（south 面 `marble → gypsum_board`、
五個面的 source 全變）——**這是 T-37 的正當修正，不是錯誤**。

所以你第一次跑治療評測、還沒改一個字的提示詞，就會被這道守門擋下。

**正確做法**（卡片已明文）：治療模式下把這道守門**改成產差異表**，不是 exit 1；
**預設模式的行為一個字都不能弱化**（Opus 會實測：預設模式竄改後仍必須擋）。
**錯誤做法（紅旗，會被退回）**：去改 T-33 凍結快取、把 TunnelToHell 從
`GATE_ITEMS` 拿掉、或把守門整個註解掉。

### 💣 地雷 C：驗收基線不是 T-36 的那組數字，要自己重算

卡片寫「驗收基線＝T-37 的再基線表」，但 `output/equirect_fix/REPORT.md`
**只有漂移表，沒有重算後的正確率**。而 T-37 確實改動了 TunnelToHell：
表 3 顯示 south 面從 `True → False`，且有兩個面從 `clip` 組移到 `fallback` 組。

也就是說 **T-36 的頭條數字（clip 面 11/21＝52.4%、非 proxy 31/60＝51.7%）在現行碼上已經過期**。
以那組數字當基線去宣稱「上升」，等於灌水。

**正確做法**：改提示詞**之前**，先用未改動的現行碼跑一次治療 harness，
得到「T-38 起算基線」寫進 REPORT，之後所有「上升」都對這組數字講。
（我用表 3 推算大約是 clip 10/19、非 proxy 30/60、floor 4/13 不變——
**這只是推算，必須由程式重算為準，不得直接抄進報告**。）

---

## §4 T-38 的範圍紅線（超出就是退回）

- ✅ 可動：`src/image_reverb/surfaces.py` 的 `CLIP_MATERIAL_PROMPTS`／
  `CLIP_OOD_PROMPTS` **字典的字串值**
- ✅ 可動：評測 harness（擴充 `t36_clip_accuracy.py` 加治療模式，或另立
  `t38_treatment_eval.py` 引用其模組）
- ❌ 不許動：候選 id 的增減（T-39 的事）、門檻 0.4、
  `classify_region_material()`／`compute_materials_confidence()` 的邏輯、
  `data/material_ground_truth.json`、`data/materials.json`
- ❌ 不許動：`SPEC.md`／`ROADMAP.md`／`WORKFLOW.md`／`output/mvp_acceptance/`／
  `output/material_round/`／`output/clip_accuracy/`
- ⚠️ **過擬合紅線**：提示詞必須是**材質的一般性描述**，不得夾帶特定場地或特定
  照片的特徵（例如「壁球場的西牆」「這張車內照的地板」）。Opus 會逐條讀提示詞內容。
  78 面既是調參集也是驗收集、沒有 held-out set——這件事要誠實寫進 REPORT。

---

## §5 驗收門檻（卡片事前寫死，防止「調到哪算哪」）

1. 端到端六面材質正確率**必須上升**（對 §3 地雷 C 重算後的基線）
2. floor 角色正確率**必須上升**；in-set 誤判面數**不得上升**
3. clip／fallback／out_of_domain 分組數字照列（分母會移動，如實記錄，不當門檻）
4. 基線變化表（共同鐵則 8）＋ gate 放行變化清單，放行清單要附
   「是否 T-17 已知錯誤輸出」欄
5. **臥室紅旗**（共同鐵則 7）：`bedroom_ai_generated` 的 overall gate
   若從「擋」變「放」→ 🔴 停、回 Fable。不得為了避開紅旗而故意不改善臥室

**共同鐵則（每張卡都要跑，全文在 TASKS.md「Phase 1.9 插卡」節）**：
15 支 `scripts/test_*.py` 全 exit 0；六條交付 IR MD5 一條都不許變；
`ir_metrics.py` 零 diff；新測試對舊碼必須實測 fail；13 張基線變化表由程式產出。

---

## §6 你（使用者）要做的事

**T-38 不需要你做判斷，只要開視窗貼 Prompt。** 依 [WORKFLOW.md](WORKFLOW.md)：

1. 開 **Sonnet** 視窗，貼：

```
執行 TASKS.md 的任務 T-38。先讀 CLAUDE.md、HANDOFF_T38.md 和該任務卡的全部內容再動工，完成後執行任務卡裡的「自我檢查」，最後照 WORKFLOW.md 第 4 節做收工程序。
```

2. Sonnet 回報 🔵 待驗證後，開 **Opus** 視窗，貼 WORKFLOW.md §2.2 的驗證 Prompt
   （把 T-XX 換成 T-38）。
3. Opus 說 ✅ 通過 → 進 T-39；說 ❌ 退回 → 再開 Sonnet 視窗修。

⚠️ **下一張 T-39 才需要你本人參與**：16 個 proxy 面的 ground truth 重對映確認
（比 T-36 的 78 面小很多，估 10 分鐘級）。到時候會另外說明。

---

## §7 卡關怎麼辦

- **Sonnet 同一件事連續 3 次嘗試失敗** → 停，在 TASKS.md T-38 卡狀態寫 `🔴 卡關`＋原因，
  只 commit 文件（`docs: T-38 卡關紀錄`），請使用者開 Fable 視窗。
- **正確率調不上去** → 這是**允許的結果**，不是失敗。如實記錄輪次軌跡與最終數字，
  狀態寫 🔴 卡關或照實回報，讓 Fable 決定要不要直接跳 T-39（擴充候選材質）。
  **絕對不許為了達標而改 ground truth、改驗收口徑、或只挑錯的面調提示詞。**
- **13 張的三軸 confidence 出現非預期漂移** → 紅旗，立刻 🔴 停（共同鐵則 8）。
- **臥室從擋變放** → 🔴 停，回 Fable（共同鐵則 7）。

---

## §8 T-38 之後（給下下個視窗）

- **T-39**（候選材質集擴充，需使用者參與）：新增材質要有建築聲學公開出處，
  **不得自創 α 數字**；既有 12 種材質的 α 逐位元不變；地雷 #13（softmax 沒有
  「以上皆非」）→ 門檻敏感度分析必須重跑。
- **T-42**（gate 交易式輸出）／**T-43**（T-17 產物溯源）：
  **任何新的正式盲聽必須等這兩張 ✅ 之後**——現存盲測素材的 MANIFEST 記的是
  `git_revision: d958b3c`，舊的 2/5 不能宣稱屬於現行碼。
  例外條款：若 T-39 因等使用者而停滯，T-42 可提前執行（檔案範圍不相交），
  提前時要在交接筆記註明。
- **收尾複評**：回 Fable，一併評估「陳設換算公式修正輪」要不要開
  （裁決 T-33-A 把重啟評估點寫死在這裡）。

---

## §9 一分鐘查證本檔是否過期

```bash
git log --oneline -3 && head -5 DEV_LOG.md
```

本檔寫於 `d590c5b T-41: 驗證通過`、DEV_LOG 最新一筆為 `2026-08-31 (83)`。
若已有更新的紀錄，以 DEV_LOG.md 與 TASKS.md 為準。
